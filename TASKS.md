## Phase 1: Context Foundry Visualizer

- [x] 1.1: Build a single self-contained HTML file at visualizer.html that visualizes the Context Foundry architecture as an interactive diagram. Include all CSS and JS inline -- no external dependencies, no other files. The visualization should show: (1) the build loop pipeline (Planner -> Builder -> Reviewer -> Fixer -> Git Commit) with animated flow between stages, (2) the module map (agent.rs, app.rs, studio.rs, config.rs, prompts.rs, patterns.rs, task.rs, tui.rs, git.rs, utils.rs, update.rs) with clickable nodes that expand to show each module's role, (3) the pattern learning cycle (extract -> store -> match -> inject), and (4) the studio mode as a separate interactive flow (prompt -> scan -> smooth -> run providers -> capture artifacts). Use modern CSS (grid, transitions, animations) and vanilla JS for interactivity. Make it visually polished with a dark theme. Read the actual source files to get accurate descriptions -- do not guess.

## Phase 2: app.rs Refactor (Cross-Model Build Loop)

- [x] T2.1: Extract AppState, AppPhase, AppEvent, LoopEvent, PlanningOutcome, PendingTransition, StartupState, EditorState, and PlanningState into src/app/state.rs. Re-export from app.rs via pub use. All existing code must compile unchanged after the move.
- [x] T2.2: Move the app.rs test block (all #[cfg(test)] tests) into src/app/tests.rs as a child module. Import needed types and functions from sibling modules. All 194 tests must still pass.
- [x] T2.3: Extract startup handlers (handle_startup_event, handle_startup_key, handle_startup_mouse, handle_startup_mouse_at, handle_startup_intent_input, move_startup_selection, set_startup_selected_action, scroll_startup_content, adjust_scroll_offset, selected_startup_action, activate_startup_action, enter_home_surface, enter_startup_surface, enter_startup_surface_for_scenario, detect_startup_scenario, classify_plan_status, and related helpers) into src/app/startup.rs.
- [x] T2.4: Extract editor handlers (handle_editor_event, handle_editor_key, load_editor_state, save_editor) into src/app/editor.rs. Keep the editor inside startup.rs if it is too small to deserve its own file; the builder should judge.

## Phase 3: Polish

- [x] T3.1: Set builder_model to "codex-5.4" and fixer_model to "codex-5.4" in the Default impl for Config (config.rs). The TUI agent status line shows BUILDER () with empty parens because Codex roles have an empty model string. After this change the TUI should show BUILDER (codex-5.4) instead. Update any tests that assert on default model values.

- [x] T3.2: Add a hot-inject input to the Running phase TUI. When the user presses 'i' during the build loop, show a single-line text input overlaid on the log pane. On Enter, append the text as a new task line (- [ ] H1.1: <user text>) to TASKS.md and dismiss the overlay. On Esc, cancel without writing. The loop already re-parses TASKS.md before each task, so the injected task will be picked up at the next task boundary. Do not use a mouse target for this -- keyboard only to avoid accidental activation. [SPID!]

- [x] T3.3: Move the "What This Drives" flow diagram from the bottom-right content area to directly below the Actions list on the left-hand side of the startup screen. Currently the flow diagram (showing SPEC.md -> TASKS.md -> build loop) renders inside startup_content_sections as a sub-area beneath the preview. It should instead render as a small block underneath the Actions list in the left column of startup_layout, so the user sees the actions and the flow context together without looking across to the right pane. The right pane should be fully dedicated to the preview content.

- [x] T3.4: Add a real-time dashboard view to the Running phase TUI, accessible via the 'd' hotkey (toggles between the current agent-output view and the dashboard; press 'd' again to return). The dashboard should include: (1) A hero section showing a subway-map style diagram of the build loop pipeline (Planner -> Builder -> Reviewer -> Fixer -> Git Commit -> next task or Discovery), rendered with box-drawing characters, where the currently active stage pulses or highlights with a distinct color and the completed stages dim. Show the provider name on each stage (e.g. "Planner (Claude opus)" / "Builder (Codex)"). (2) A progress summary: tasks completed vs total from TASKS.md, shown as a filled/empty bar and percentage; list of recently completed task IDs with pass/WIP status; the current task description; the next queued task. (3) Git stats: number of commits made this session (count feat() and WIP() separately), dirty file count. (4) Pattern stats: total patterns in the global patterns directory, patterns matched for the current task, patterns learned this session. (5) Provider health: show Claude and Codex with a connected/disconnected/rate-limited indicator based on the last agent exit status for each provider. (6) Session config: project directory, planner/builder/reviewer/fixer/discovery provider+model pairs from .foundry.json, timeout, pause settings. (7) Timing: elapsed time for current task, elapsed time for current agent, total session runtime. Use the existing TUI color scheme. The dashboard reads from AppState fields that the loop already maintains (completed_count, total_count, current_task, current_agent, log_messages, etc.) plus a few new counters (session commit counts, pattern stats) that should be added to AppState. Render with ratatui widgets (Gauge for progress, Table for config, Paragraph for the loop diagram). This is a read-only view -- no input handling beyond 'd' to toggle back.

- [x] T3.5: Add automatic provider fallback to the build loop. When a Codex agent fails with a rate-limit or subscription-limit error (detect by checking AgentExitKind and the failure_message for rate/limit keywords), the loop should retry the same agent step using Claude as the fallback provider instead of failing the task. Implement this in run_agent() in agent.rs: after the Codex retry loop exhausts its attempts or detects a rate-limit failure, construct a new run_agent call with ModelProvider::Claude and the reviewer_model (opus) as the fallback. Log the fallback clearly (e.g. "Codex rate-limited, falling back to Claude opus"). This should be transparent to the build loop -- build.rs and review.rs do not need to change.

## Discovery Round 1

- [x] D1.1: Fix unchecked indexing panic in studio/state.rs:656 -- selected_execution_contract() uses direct array index which panics if execution_contracts is empty or index is stale; use .get() with expect or return Result, and add bounds validation in set_selected_execution_contract_index() at line 660
- [x] D1.2: Replace hardcoded 'push origin main' in git.rs:111 with dynamic branch/remote detection -- use 'git symbolic-ref --short HEAD' for current branch and 'git remote' to detect the configured remote, preventing silent push failures for non-main branches or missing origin
- [x] D1.3: Replace .expect() on mutex locks in agent.rs (lines 178, 195, 337) with unwrap_or_else(|p| p.into_inner()) to prevent cascade panics when a thread panics while holding the provider progress lock


## Phase 5: Doubt Loop TUI Integration

- [x] T5.1: Add a "Design with review" startup action (available in all scenarios except EmptyProject). When selected, open the intent input. On Enter, run the orchestrator loop (src/orchestrator.rs) through the TUI Planning phase -- showing proposer/reviewer iterations in the agent output panel, iteration count and finding count in the header, and the final artifact in a preview on completion. Return to startup with the output in .buildloop/orchestrator-output.md. Do not auto-write to TASKS.md or SPEC.md.

- [x] T5.2: Update the Planning phase header to show the orchestrator iteration count and role (e.g. "Iteration 2/3: Reviewing with Codex codex-5.4" or "Iteration 1/3: Proposing with Claude opus"). The existing planning header shows "Planning -- label" and "PLANNER (model) | time | events". For orchestrator mode, replace "Planning" with "Design" and show which sub-agent (proposer or reviewer) is active. Use a new field in PlanningState to distinguish orchestrator mode from regular planning.

- [x] T5.3: Add a "Review findings" panel to the TUI that appears when the orchestrator completes with unresolved findings. Show the findings list with severity colors (high=red, medium=yellow, low=gray), the validated claims in green, and the iteration history. This panel should be scrollable and accessible via a new key hint in the status bar.

- [x] T5.4: Wire the orchestrator's on_event callback into the TUI event system so proposer/reviewer agent output streams live to the agent output panel during the design loop. Currently the orchestrator uses run_agent_and_capture() which collects output silently. Instead, forward AgentOutputEvent to the TUI's event_tx channel, and add a separator line between proposer and reviewer output (e.g. "--- Reviewer pass ---").

- [x] T5.5: Add the orchestrator config fields (orchestrator_proposer_provider, orchestrator_proposer_model, orchestrator_reviewer_provider, orchestrator_reviewer_model, orchestrator_max_iterations, orchestrator_accept_policy) to the Session Config table on the dashboard view. Show them in a separate "Orchestrator" section below the existing role table.

- [x] T5.6: Add test coverage for the orchestrator TUI integration: startup action creates the right transition, planning header shows orchestrator mode, iteration count updates correctly, findings panel renders severity colors, and agent output forwarding produces visible events.

## Phase 6: Cleanup and Hardening

- [x] T6.1: Remove the header log line duplication during Planning phase. Currently the header shows the planning label on line 2 AND the most recent log message on line 4, which repeats the same "Planning started" text. The log line should be suppressed during Planning phase since the header already provides that context. (Fix is already in tui.rs but needs the build loop to pick it up.)

- [x] T6.2: Add regression tests for the state file backup/restore in build.rs: test that backup captures TASKS.md, SPEC.md, CLAUDE.md, and .buildloop/current-plan.md; test that restore recovers deleted files; test that restore recovers truncated files (under 10 bytes); test that restore does NOT overwrite current-plan.md when it was legitimately rewritten by the planner; test that write failures produce warning log messages.

- [x] T6.3: Add regression tests for the semantic pattern matcher in embeddings.rs: test duplicate pattern_id cache collision does not occur with model:hash keys; test empty patterns list skips Ollama; test circuit breaker cooldown and recovery; test that match_patterns_semantic returns keyword-only results when Ollama is unavailable.

- [x] T6.4: Add regression tests for the orchestrator in orchestrator.rs: test that is_accepted does not trust status="clean" when findings exist; test case-insensitive severity matching; test the full orchestration loop with mock agent responses; test max iterations termination; test JSON extraction from markdown-fenced responses.

- [x] T6.5: Tighten the embeddings.rs stale cache test: instead of just comparing strings, exercise the actual cache load/discard logic by writing a cache file with a wrong model name, loading it, and verifying the stale entry is discarded and re-embedded.

## Discovery Round 2

- [x] D2.1: Fix orchestrator run_agent_and_capture in orchestrator.rs:487 hardcoding timeout to 600 instead of using config.agent_timeout_secs -- pass the configured timeout through OrchestratorConfig so user-configured timeouts are respected
- [x] D2.2: Fix extract_json brace-counting parser in orchestrator.rs:236-255 not skipping braces inside JSON string values -- when artifact_text contains code with braces (e.g. fn main() { }), the depth counter finds the wrong closing brace and produces truncated JSON, causing parse fallback
- [x] D2.3: Pass shutdown token to orchestrator agents in orchestrator.rs:484 instead of None -- currently orchestrator proposer/reviewer agents are uncancellable, unlike build loop agents which all receive the shutdown Arc, so a hung design-with-review agent blocks indefinitely
- [x] D2.4: Narrow should_restart_docker keyword matching in build.rs:562-570 -- the keywords "integration" and "scaffold" are too broad, triggering unnecessary Docker compose down/up cycles for tasks like "Add integration tests" or "Scaffold test fixtures" that have nothing to do with Docker
- [x] D2.5: Add explicit match arm for "no-high-medium" in orchestrator accept_policy parser at orchestrator.rs:69-72 -- the default config value "no-high-medium" currently falls through to the catch-all wildcard arm, which happens to produce correct behavior but is fragile if new arms are added

## Discovery Round 3

- [x] D3.1: Fix race condition in embeddings.rs test test_match_patterns_semantic_returns_keyword_only_when_ollama_unavailable at line 682 -- the test uses force_cooldown() on global static OLLAMA_STATE but test_empty_patterns_skips_ollama (line 650) and test_circuit_breaker_cooldown_and_recovery (line 668) call clear_cooldown() concurrently, causing the cooldown to be cleared before is_available() is checked and returning "keyword-only" instead of "cooldown"; serialize these tests or use a per-test isolation strategy
- [x] D3.2: Fix hardcoded 600-second timeout in run_pattern_extraction at build.rs:519 -- should use ctx.config.agent_timeout_secs instead of literal 600, same class of bug as the orchestrator timeout fixed in D2.1
- [x] D3.3: Fix review loop in review.rs:65-80 not checking reviewer agent success before evaluating the report -- if the reviewer agent crashes without writing review-report.md, parse_audit_findings returns (0,0,0) and the condition verdict_pass || (high == 0 && medium == 0) evaluates to true, silently passing review; add an early return false when the reviewer agent fails

## Discovery Round 4

- [x] D4.1: Fix review loop in review.rs:83-94 silently passing review when reviewer agent succeeds but review-report.md is missing or empty -- parse_audit_findings returns (0,0,0) for missing/empty files and the condition (high == 0 && medium == 0) evaluates to true, bypassing the verdict check; add an explicit check that the report file exists and has content before evaluating findings
- [x] D4.2: Fix orchestrator.rs:424-425 using case-sensitive severity match (== "high") in the finding-count log while is_accepted at line 285 uses eq_ignore_ascii_case -- if the reviewer outputs "HIGH" or "High", the log reports 0 high findings while acceptance correctly rejects, producing misleading output; use eq_ignore_ascii_case consistently
- [x] D4.3: Fix hardcoded 300-second timeout in run_append_tasks at planning.rs:93 -- should use ctx.config.agent_timeout_secs instead of literal 300, same class of bug as D2.1 and D3.2
- [x] D4.4: Cap log_messages growth in state.rs:129 -- the Vec grows without bound during long sessions; the TUI only renders the most recent entry so old entries are invisible but consume memory indefinitely; add a cap (e.g. 500 entries) and drop oldest when exceeded

## Discovery Round 5

- [x] D5.1: Cap agent_output Vec growth in app.rs handle_agent_output -- same class of bug as D4.4 (log_messages); agent_output is pushed to at lines 934, 945, 955, 959, 964 without any bound; during long builder/reviewer runs this accumulates memory indefinitely since the TUI only renders visible lines with scroll offset; add a cap (e.g. 2000 entries) with oldest-drop in handle_agent_output, matching the pattern used for log_messages in state.rs
- [x] D5.2: Fix provider_binary_is_available in commands.rs:46 and studio/providers.rs:571 using Unix-only "which" command -- Windows has no "which" binary so this always returns false on Windows, causing ensure_required_providers_available to reject all providers; the codebase explicitly supports Windows (release.yml builds x86_64-pc-windows-msvc, update.rs has full Windows codepaths); use "where" on Windows or check PATH entries directly
- [x] D5.3: Guard against empty model string in run_agent Claude path at agent.rs:551-552 -- run_provider_session correctly skips --model when model is empty (line 269), but the Claude path in run_agent always passes --model even if model.trim().is_empty(), which sends --model "" to claude CLI; add the same is_empty guard for consistency

## Discovery Round 6

- [x] D6.1: Fix cache_dir() in embeddings.rs:186 and resolve_patterns_dir() in patterns.rs:43 using Unix-only HOME env var for path resolution -- on Windows HOME is unset so cache_dir falls through to /tmp/.foundry/cache (invalid Windows path) and resolve_patterns_dir falls through to /tmp/.foundry/patterns (also invalid); update.rs:67-74 already has the correct pattern using LOCALAPPDATA/USERPROFILE on Windows; apply the same cfg!(target_os = "windows") branching to both functions
- [x] D6.2: Add wrapper-object format support to load_patterns() in patterns.rs:56-81 -- the function only parses flat arrays (Vec<Pattern>) and single objects (Pattern) but not the documented wrapper format ({"pattern_type": "...", "domain": "...", "patterns": [...]}) used by extension pattern files (e.g. extensions/recon/patterns/recon-common-issues.json); if a wrapper-format file is placed in the global patterns directory its patterns are silently skipped; add a serde struct for the wrapper and try parsing it between the array and single-object attempts

## Discovery Round 7

- [x] D7.1: Fix launch_external_editor in app.rs:285-291 using Unix-only "sh -c" to invoke the editor -- on Windows sh does not exist so the editor feature always fails; the codebase explicitly supports Windows (release.yml builds x86_64-pc-windows-msvc, update.rs has Windows codepaths, commands.rs uses "where" on Windows); use cmd /C on Windows or call the editor binary directly without a shell wrapper, matching the platform branching pattern already used elsewhere
- [x] D7.2: Fix discovery_round counter in build.rs:106 starting at 0 every session -- when a new session starts, discovery rounds begin numbering from 1 regardless of existing rounds in TASKS.md, creating duplicate section headers ("## Discovery Round 1") and duplicate task IDs (D1.1, D2.1) that collide with IDs from previous sessions, producing confusing git commits with ambiguous IDs; scan TASKS.md at build_loop initialization for the highest existing discovery round number and start from there
- [x] D7.3: Prune stale entries from the embedding cache in embeddings.rs save_cache at line 227 -- cache entries for patterns that have been deleted or renamed are never removed, causing the cache file (~/.foundry/cache/pattern-embeddings.json) to grow indefinitely over months of use; during save_cache, remove entries whose content_hash does not match any current pattern text

## Discovery Round 8

- [x] D8.1: Add ensure_required_providers_available() call to run_design_command in orchestrator.rs:598 -- all other CLI entry points (run_tui via app.rs:41, run_headless via commands.rs:119, run_plan_mode) validate that required provider CLIs (claude, codex) are installed before spawning agents, but `foundry design` skips this check entirely; when a provider binary is missing, the user gets a cryptic PTY spawn error instead of the clear "required provider CLI not found" message that other commands produce
- [x] D8.2: Cache compiled regexes in task.rs parse_tasks (line 31) and highest_discovery_round (lines 99-100) using std::sync::OnceLock<Regex> -- parse_tasks recompiles Regex::new(r"^([A-Za-z]?\d+\.\d+):\s*") on every call, and the build loop invokes it 4+ times per task cycle (build.rs:109, 159, 222, 249); highest_discovery_round compiles two separate regexes per call; these are hot paths during long sessions with many tasks; use OnceLock or LazyLock to compile once and reuse
- [x] D8.3: Fix double to_lowercase() allocation in check_review_passed at review.rs:294-295 -- content.to_lowercase() is called twice on the same string to check for "verdict: pass" and "verdict:pass", creating two redundant heap allocations of the entire review report; extract to a local variable and reuse

## Discovery Round 9

- [x] D9.1: Add wrapper-format support to merge_patterns() in patterns.rs:216-223 -- when loading existing patterns from common-issues.json for merging, only flat array and single-object JSON formats are tried; the wrapper format ({"pattern_type": "...", "patterns": [...]}) supported by load_patterns() since D6.2 is not handled, so if the file is in wrapper format all existing patterns are silently discarded and replaced by the new ones; use the same PatternWrapper struct already defined at patterns.rs:44
- [x] D9.2: Add stop-file check between agent stages in process_task at build.rs:360-410 -- after the planner completes and before the builder starts (and between builder and reviewer), the function sleeps for pause_between_agents_secs but never checks ctx.stop_file().exists() or ctx.shutdown; if a user touches .buildloop/stop during the planner phase, the entire remaining pipeline (builder + reviewer + fixer) must complete before the stop is honored at build.rs:167; check after each agent stage and return early if stop is requested
- [x] D9.3: Switch run_agent Claude path timeout from wall-clock deadline to idle-based timeout at agent.rs:601 -- the Claude path sets deadline = Instant::now() + Duration::from_secs(timeout_secs) which kills agents that are actively producing output on long tasks; run_provider_session (used by Studio) correctly uses ProviderProgressState.no_progress_for() as an idle timeout with a separate hard deadline multiplier; apply the same idle+hard timeout pattern to the build-loop Claude path for consistency

## Discovery Round 10

- [x] D10.1: Add stop-file/shutdown check to the review loop in review.rs between fixer completion and second reviewer pass -- run_review_loop sleeps at lines 128 and 175 between fixer and the pass-2 reviewer but never checks ctx.stop_file().exists() or ctx.shutdown.load(); same class of bug as D9.2 which added stop checks between stages in build.rs but missed the review loop; if a user touches .buildloop/stop during the fixer or the inter-pass sleep, the entire second reviewer pass must complete before the stop is honored
- [x] D10.2: Sync TUI discovery_round counter with actual round numbers from TASKS.md -- state.rs:182 initializes discovery_round to 0 and app.rs:642 increments on each DiscoveryStarted event, but the build loop at build.rs:107 initializes from highest_discovery_round(); DiscoveryStarted at state.rs:265 carries no round number; the status bar at tui.rs:1753 shows session-relative rounds (1, 2, 3) instead of absolute rounds (e.g. 10, 11, 12) when resuming sessions with existing discovery rounds; either add the round number to the DiscoveryStarted event or initialize the TUI counter from TASKS.md at build loop start

## Discovery Round 11

- [x] D11.1: Add stop-file/shutdown check to the discovery cycle in build.rs:176-257 -- the discovery branch of build_loop never checks ctx.stop_file().exists() or ctx.shutdown.load(); the is_stop_requested() helper is only called inside process_task() and at line 168 after task completion; if all tasks are completed and the loop enters discovery mode, touching .buildloop/stop has no effect until a new task is discovered and fully processed; add stop checks after discovery agent completion (line 221), after the inter-cycle sleep (line 236), and after discovery git commit (line 248); same class of bug as D9.2 and D10.1 which added stop checks in process_task and review_loop but missed the discovery cycle
- [x] D11.2: Pass a shutdown token to run_design_command CLI entry point in orchestrator.rs:618-627 -- the CLI foundry design command passes None for the shutdown parameter to orchestrate(), so PTY-spawned agent subprocesses are not cancellable via the shutdown mechanism; the TUI design path (app.rs:1082) correctly passes Some(shutdown.clone()); without a shutdown token, orchestrate() line 346 short-circuits via is_some_and(false) and run_agent() line 631 does the same, leaving orphan PTY child processes on Ctrl+C since portable-pty creates a new session; create an AtomicBool, install a ctrlc/signal handler to set it, and pass Some(Arc) through to orchestrate()
- [x] D11.3: Make embedding cache write atomic in embeddings.rs save_cache_to at line 247 -- std::fs::write() is non-atomic so the cache file can be truncated if the process is killed mid-write or two foundry sessions write concurrently; patterns.rs merge_patterns already uses the correct pattern (write to .tmp then fs::rename at lines 255-256); apply the same tmp-file-then-rename strategy to save_cache_to for consistency

## Discovery Round 12

- [x] D12.1: Fix background pattern extraction overwriting TUI agent state in build.rs:517-529 -- the fire-and-forget pattern extraction spawned via tokio::spawn sends AgentStarted(Discovery) at line 566 and AgentDone at line 589 through the same event channel (tx) as the build loop; if extraction overlaps with the next task, handle_event at app.rs:614-619 calls set_agent() which clears agent_output, resets scroll_offset, and overwrites current_agent, causing the planner/builder output to vanish and the TUI header to briefly show DISCOVERY; also pushes Discovery into task_stages_seen at line 616 which gets saved to the wrong task's pipeline history at line 631; fix by either suppressing AgentStarted/AgentDone from background extraction or using a distinct background-only log event that does not trigger set_agent
- [x] D12.2: Install a signal handler in run_headless at commands.rs:112-221 to set the shutdown AtomicBool on Ctrl+C -- the headless mode creates a shutdown Arc<AtomicBool> at line 117 but never installs a signal handler to set it; run_agent checks shutdown at agent.rs:631-641 and process_task checks it between stages at build.rs:410,467, but neither fires without a handler; in headless mode Ctrl+C terminates the process abruptly and orphaned PTY child processes may remain running since portable-pty creates a new session; same class of bug as D11.2 which fixed the identical problem for foundry design; add tokio::signal::ctrl_c handler matching the pattern used in orchestrator.rs:618-623

## Discovery Round 13

- [x] D13.1: Install a signal handler in run_plan_mode at planning.rs:131-135 to set the shutdown AtomicBool on Ctrl+C -- same class of bug as D11.2 (foundry design) and D12.2 (foundry run --headless); run_plan_mode creates Arc<AtomicBool>::new(false) at line 135 but never wires it to a signal handler; agents receive Some(ctx.shutdown.clone()) at line 202 via run_gap_analysis_iteration but the flag is never set to true; on Ctrl+C the process terminates abruptly and orphaned PTY child processes may remain; add tokio::signal::ctrl_c handler matching the pattern now used in commands.rs:122-125 and orchestrator.rs:618-623
- [x] D13.2: Replace hardcoded "opus" in Codex-to-Claude fallback at agent.rs:527 with the callers configured Claude model -- when Codex rate-limits and run_agent falls back to Claude, it always uses "opus" regardless of the users .foundry.json configuration; if the user configured a different Claude model for the role (e.g. sonnet for cost control), the fallback ignores it and uses the more expensive model; add a fallback_model parameter to run_agent or resolve the correct role model from the config at the call site in build.rs process_task
- [x] D13.3: Log a warning in Config::load at config.rs:152-153 when .foundry.json exists but fails to read or parse -- both read_to_string and serde_json::from_str use unwrap_or_default() which silently replaces a corrupt or unreadable config with defaults; users get no indication their settings were ignored, making it hard to debug why configuration changes have no effect; add eprintln warnings on read failure and parse failure before falling back to defaults

## Discovery Round 14

- [x] D14.1: Fix race condition between commit_inject_task in app.rs:851-950 and task::mark_done in task.rs:82-91 on TASKS.md -- both functions read-modify-write TASKS.md without coordination; commit_inject_task runs from the TUI event handler (main task) while mark_done runs from the build_loop (spawned tokio task at app.rs:337-339); if a user submits an injected task at the moment the build loop completes a task, one write can overwrite the other, silently losing the injected task or reverting a completed task to unchecked; add a file-level coordination mechanism (e.g. tokio::sync::Mutex around TASKS.md writes, or route inject writes through the build loop via a channel)
- [x] D14.2: Add explicit return false after the pass-2 failure branch in run_review_loop at review.rs:212-223 -- when the second reviewer pass still has high/medium findings, the code at line 218 sends TaskReviewResult with passed=false but does not return; execution falls through to line 226 which calls check_review_passed and re-reads the report file; if the reviewer wrote both high findings in the JSON block and "Verdict: PASS" in the text (inconsistent LLM output), the function returns true while the TUI already recorded false; the fall-through is also wasteful (redundant file read); add return false after line 222 to match the early-return pattern used in all other exit paths of this function
- [x] D14.3: Consolidate duplicate is_stop_requested helper functions in build.rs:299-303 and review.rs:13-17 into a method on RunContext -- both are identical (check stop_file.exists() or shutdown.load()), but maintaining two copies risks divergence if new stop conditions are added; move the logic to a pub method on RunContext in context.rs so build.rs, review.rs, and any future modules use the same implementation

## Discovery Round 15

No new tasks discovered.

## Discovery Round 16

- [x] D16.1: Remove unreachable dead code at review.rs:220 -- after D14.2 added return false at line 216 in the pass-2 failure branch, the check_review_passed call after the for loop (line 220) is unreachable; every path through for pass in 1..=2 returns inside the loop body (pass 1 returns or continues to pass 2, pass 2 always returns); replace line 220 with false or unreachable!("all paths return inside the review loop") to make the control flow explicit
- [x] D16.2: Move "compose" from primary Docker keywords to qualified keywords in should_restart_docker at build.rs:624 -- "compose" is in the primary keyword list that always triggers docker compose down/up, but it is a common English word that appears in non-Docker task descriptions (e.g. "Compose validation error messages", "Compose a response template"); D2.4 narrowed "integration" and "scaffold" to require an infra qualifier but missed "compose"; move "compose" to the broad_keywords list so it only triggers Docker restart when paired with an infra qualifier like "docker", "container", or "service"
- [x] D16.3: Cap task_history HashMap growth in state.rs:138 and app.rs:630,690 -- task_history grows without bound as tasks accumulate across long sessions with many discovery rounds; unlike log_messages (capped at 500 per D4.4) and agent_output (capped at 2000 per D5.1), task_history has no size limit; each entry is small but sessions running hundreds of tasks can accumulate thousands of entries; add a cap (e.g. 200 entries) and evict oldest entries when exceeded

## Discovery Round 17

No new tasks discovered.

## Discovery Round 18

- [x] D18.1: Use atomic write (tmp+rename) for studio file persistence instead of direct fs::write -- history.rs:45 (prompt history JSON), contracts.rs:189 (selected contract), contracts.rs:225-226 (new contract + sidecar), attachments.rs:606 (attachment specs JSON), ui/input.rs:751 (editor choice) all use fs::write() directly; a crash or power loss mid-write can leave partially-written files that fail to parse on next load; this is the same class of bug D11.3 fixed in embeddings.rs; extract a shared atomic_write_file() helper (write to .tmp in same dir, then fs::rename) and use it in these 6 locations; providers.rs:508 and session.rs:244 are best-effort (let _ =) and lower priority but should also be converted for consistency

## Discovery Round 19

- [x] D19.1: Use atomic_write_file from utils.rs for TASKS.md writes in task.rs:90 and app.rs:932,939 -- mark_done() at task.rs:90 uses fs::write() which can corrupt TASKS.md on crash mid-write; commit_inject_task() at app.rs:932 uses fs::write() to rewrite the file and at app.rs:939 uses OpenOptions::append() which is also non-atomic; TASKS.md is the most critical state file in the build loop (losing it means losing all progress tracking); D18.1 fixed the same class of bug in studio files and D11.3 fixed it in embeddings.rs; the atomic_write_file() helper already exists at utils.rs:33; for the append case in commit_inject_task, read-then-atomic-write is safer than non-atomic append
- [x] D19.2: Use atomic_write_file from utils.rs for SPEC.md creation in app.rs:483 and orchestrator output in orchestrator.rs:594 -- seed_spec_from_brief() at app.rs:483 uses std::fs::write() to create SPEC.md from the brief; write_orchestrator_output() at orchestrator.rs:594 uses std::fs::write() for orchestrator-output.md; both are user-visible artifacts where partial writes would produce corrupt files; same class of bug as D19.1 and D18.1; use the existing atomic_write_file() helper
- [x] D19.3: Use atomic_write_file from utils.rs for state file restoration in build.rs:87 -- restore_state_files() uses std::fs::write() to restore backed-up state files (TASKS.md, SPEC.md, CLAUDE.md, current-plan.md); a crash during restore could corrupt the very file being restored, which is ironic since the restore function exists to recover from corruption; the atomic tmp+rename pattern prevents this; the backup read at build.rs:82 is fine but the write at line 87 should use atomic_write_file()

## Discovery Round 20

No new tasks discovered.

## Discovery Round 21

No new tasks discovered.

## Discovery Round 22

- [x] D22.1: Fix UTF-8 panic in inject input display at tui.rs:113-114 -- the slice input[input.len() - max_text..] can panic on a non-char-boundary when inject_input contains multi-byte characters (emoji, CJK, accented chars) because max_text derives from terminal column width (not byte count); app.rs:825-827 accepts arbitrary KeyCode::Char(c) and app.rs:802-804 accepts pasted text so multi-byte input is expected; replace the raw slice with truncate_str_from_end(input, max_text) from utils.rs:21 which already handles char boundary alignment
- [x] D22.2: Guard the Unix-only import in config.rs:213 with cfg(unix) -- use std::os::unix::fs::PermissionsExt is unconditionally imported at the top of the #[cfg(test)] mod tests block, which prevents the entire config test module from compiling on Windows; only the load_warns_on_unreadable_file test at line 274 uses it; move the import inside that test function and add #[cfg(unix)] to the test, matching the pattern used in build.rs:852 and attachments.rs:907-908

## Discovery Round 23

- [x] D23.1: Fix UTF-8 panic in pipeline subway map model label at tui.rs:199 -- display[..12].to_string() slices by byte index which panics if display contains multi-byte UTF-8 characters at the boundary; display is built from provider name and model string at tui.rs:196 which are user-configurable via .foundry.json; same bug class as D22.1 (inject input) but at a different code location; replace with truncate_str(&display, 12) from utils.rs:3 which already handles char boundary alignment
- [x] D23.2: Fix Codex-to-Claude fallback passing Codex-specific model string at agent.rs:528 -- when Codex fails and should_fallback_to_claude() returns true, run_agent() recursively calls itself with ModelProvider::Claude but passes the original model parameter verbatim; if the role is configured with a Codex-specific model name (e.g. the former default "codex-5.4" or any OpenAI model ID), Claude CLI receives an invalid --model argument and the fallback also fails; the fallback should pass an empty string "" to let Claude CLI use its default model, matching T3.5's intent of transparent fallback

## Discovery Round 24

No new tasks discovered.

## Discovery Round 25

- [x] D25.1: Fix UTF-8 slicing panic in parse_reviewer_output fallback at orchestrator.rs:228 -- the expression &response[..response.len().min(200)] slices by byte index which panics if byte 200 falls in the middle of a multi-byte UTF-8 character (emoji, CJK, accented chars); reviewer output can contain arbitrary text so this is reachable; same class of bug as D22.1 (inject input) and D23.1 (pipeline label) but at a different code location; replace with truncate_str(&response, 200) from utils.rs which is already imported at line 12

## Phase 7: Foundry Overview Page

- [x] T7.1: Create foundry-overview.html with a dark-themed layout showing the Context Foundry brand header, a brief tagline, and four main sections: The Build Loop, The Review Gate, The Doubt Loop, and Discovery + Patterns. All CSS and JS inline, no external dependencies. Single file. Read the actual source code to get accurate descriptions.
- [x] T7.2: Add an interactive Build Loop section with clickable stage boxes (Planner > Builder > Reviewer > Fixer > Commit). Clicking a box shows a tooltip explaining what that stage does. Add a simple animation showing flow through the pipeline. Below the pipeline, show the Review Gate: how the reviewer runs up to 2 passes, the fixer runs between passes, and PASS/FAIL verdicts determine whether the commit is feat() or WIP(). This is a loop within the build loop (for pass in 1..=2).
- [x] T7.3: Add a Doubt Loop section showing the Proposer and Reviewer iterating with arrows between them. Show iteration count (up to 3), findings badge with severity colors, and acceptance policy. This is a real loop (for iteration in 1..=max_iterations). Below it, show Discovery (not a loop -- it's what the build loop does when the task queue is empty: scan repo, append new tasks, continue) and Pattern Learning (not a loop -- a pipeline stage that extracts patterns after each task, stores them, and injects matched patterns into future prompts via keyword + semantic similarity).
- [x] T7.4: Add a Task Queue section showing sample tasks with checkmarks, prefix colors (T=white for planned, D=blue for discovered, H=yellow for human-injected), and the compact PBRF pipeline indicator per task. Make the whole page scrollable and responsive.

## Phase 8: Performance and Efficiency (Issues #199, #201-#206)

- [x] T8.1: Adaptive inter-agent pauses (#202). In build.rs, track whether the last agent call was rate-limited (the rate_limit_event is already detected and logged). If no rate limit was hit on the previous call, skip the pause_between_agents_secs delay entirely (or use a minimal 500ms delay). If rate limited, keep the configured pause or increase it. Apply the same logic to pause_between_tasks_secs. Add a config field adaptive_pauses (default true). This change touches the sleep() calls at build.rs lines where tokio::time::sleep(pause_between_agents_secs) appears (between planner/builder, builder/reviewer, reviewer/fixer) and the pause_between_tasks_secs sleep after task completion.

- [x] T8.2: Model routing by task complexity (#203). Add a lightweight task complexity classifier that runs before the planner. Classify tasks as simple/medium/complex based on description length, keyword signals (rename/add/fix vs architect/redesign/implement), and file count hints. Add model tier config to .foundry.json with per-complexity model overrides for planner/builder/reviewer. Simple tasks use faster/cheaper models (sonnet), complex tasks use opus. Default tier config should be sensible without user configuration. The classifier should be a pure function, not an LLM call.

- [x] T8.3: Skip planner for simple tasks (#204). When the complexity classifier from T8.2 marks a task as "simple", skip the planner stage entirely. Instead of writing current-plan.md and having the builder read it, pass the task description directly to the builder prompt as its specification. Add a builder_direct_prompt() function to prompts.rs that formats the task description as a builder instruction without requiring a plan file. The builder should still run cargo test / npm test as verification. Add a config field skip_planner_for_simple (default true).

- [x] T8.4: Diff-only review mode (#205). After the builder runs, capture the git diff (git diff HEAD or git diff --cached). Pass the diff inline in the reviewer prompt instead of listing file paths for the reviewer to read individually. For new files (no diff available), include the full content. Add a config field review_mode ("full" or "diff-only", default "diff-only"). Update the reviewer_prompt() function in prompts.rs to accept either a file list or a diff string. The reviewer should still run build/test commands but reads the diff instead of globbing through files.

- [x] T8.5: Planner look-ahead (#201). While the builder is running task N, spawn the planner for task N+1 in parallel. The look-ahead planner writes to .buildloop/plan-{task_id}.md instead of current-plan.md. When task N completes and task N+1 starts, check if plan-{task_id}.md exists. If it does, rename it to current-plan.md and skip the planning phase. If it doesn't (look-ahead failed or was cancelled), plan normally. Cancel the look-ahead if discovery runs, if the user injects a task, or if the task queue is reordered. Only look ahead by 1 task.

- [x] T8.6: Smart discovery cooldown (#206). After the task queue empties, check when the last task was added and how it was added. If all completed tasks were from a recent "Describe work" or manual inject (not from a previous discovery), skip the first discovery scan and wait discovery_cooldown_minutes (default 5, configurable in .foundry.json) before scanning. If the previous discovery round found zero new tasks, increase the cooldown exponentially (5 min, 15 min, 30 min) up to a max. Reset the cooldown when the user adds tasks manually.

- [x] T8.7: Parallel task execution for independent tasks (#199). This is the largest change. Add a dependency analyzer that groups pending tasks into parallel batches. Tasks within the same phase section that don't reference each other's output files can run in parallel. For each parallel batch: fork the repo into N git worktrees (one per task), run each task's full pipeline (planner > builder > reviewer > fixer) concurrently, then merge worktrees back sequentially with conflict detection. Update the TUI to show multiple active tasks with multiplexed agent output. Add a config field max_parallel_tasks (default 3). This task depends on T8.1-T8.6 being stable first.

## Phase 9: Restyle Overview Page

- [x] T9.1: Restyle foundry-overview.html to match the design system patterns. Replace the current color palette with the exact CSS custom properties from the design patterns: --bg: #1a1a2e, --surface: #16213e, --surface-hover: #1a2744, --border: #2a3a5c, --text: #e8e8e8, --text-muted: #8892a4, --accent: #E3734B (Claude Code orange). Remove ALL per-section accent colors and use only #E3734B as the single accent. Remove any gradients, glows, or glassmorphism. Shadow must be 0 1px 3px rgba(0,0,0,0.3) only. Border-radius 8px everywhere. Typography: h1 1.75rem, h2 1.25rem, body 0.9rem, small 0.8rem. The page should look premium and restrained, not like a colorful AI demo.

## Phase 10: Startup Screen Redesign -- File Explorer

- [x] T10.1: Replace the startup Actions panel with a project file explorer in src/tui/startup.rs. Show the directory tree (flat, all folders expanded) in the left panel using ratatui List widget. Each entry shows the file/folder name with indent based on depth. Clickable entries (mouse + arrow keys) select a file and display its content in the right preview panel. Limit the tree to 3 levels deep and exclude node_modules/, target/, .git/, venv/, __pycache__/. Highlight Context Foundry files in orange (#E3734B): TASKS.md, SPEC.md, CLAUDE.md, .buildloop/ and everything inside it. All other files use default gray/white. The explorer reads the directory tree once at startup and caches it on StartupState. Add a "What do you want to do?" input prompt below the explorer with rotating placeholder suggestions that cycle every 3 seconds: "Scan this codebase and find improvements...", "Add authentication with JWT tokens...", "Create a REST API for...", "Fix the failing tests in...", "Add Docker Compose setup...", "Refactor the frontend to use...", "Add CI/CD pipeline with GitHub Actions...", "Improve error handling across...", "Add real-time updates with WebSockets...", "Write comprehensive tests for...". When the user types and presses Enter, route to the existing AI task creation flow (AppendTasks for QueueReady/QueueComplete, gap analysis for NeedsQueue/EmptyProject). If the input is empty on Enter, run a general scan. The file explorer stays visible during input -- the preview panel shows the selected file content. After submission, transition to the running phase (dashboard). Keep the existing Tab toggle for the dashboard view, the 'm' mode toggle, and the Esc quit. Remove the old Actions list, the "How This Works" flow diagram panel, and the scenario-specific intent panels -- the file explorer + input prompt replaces all of them. [SPIV]



## Phase 11: Norton Commander Explorer + Scrollable Panes

- [x] T11.1: Norton Commander-style file explorer -- Add collapsible folders (expanded:bool on FileEntry, triangle icons ▼/▶, click or Enter to toggle, skip rendering children of collapsed dirs), file detail columns (name | size | date/time from std::fs::metadata, human-readable sizes like 1.2K/2.1M), and click-to-edit integration (Enter on file suspends TUI and launches $EDITOR/nano via existing open_editor pattern in app.rs:310-338, resume TUI and reload preview on exit). FileEntry needs new fields: expanded:bool, file_size:u64, modified:Option<SystemTime>. Sort dirs first then alpha. Arrow keys navigate visible entries only (skip children of collapsed dirs). Reference: GitHub issue #221. [SPID]

- [x] T11.2: Scrollable focused panes and Tab toggle during running phase -- Copy the Studio pattern (src/studio/ui/input.rs:531-651, src/studio/ui/layout.rs:431-498) into the startup and running TUI screens. Add FocusedPane enum to AppState with variants for each pane (Explorer, Preview, AgentOutput, TaskQueue, PatternsLearned). Click a pane to focus it (bold+thick border via pane_border_style/pane_border_type helpers). Mouse wheel scrolls the focused pane (3 lines per event, clamped to max_scroll=content_len-visible_lines). Hit-test with rect_contains(area, column, row). Startup panes: Explorer (already has arrow scroll, add mouse wheel), Preview (add scroll offset + mouse wheel). Running panes: AgentOutput (already has scroll_offset, add click focus), TaskQueue (already has task_queue_scroll, add click focus), PatternsLearned (add scroll). Static panes (Pipeline, Stats) don't need scroll. ALSO: Enable the Tab key during Running phase to toggle between the dashboard view and the file explorer view. Currently Tab only works on the Startup screen (show_run_view toggle in handle_startup_key). During Running phase, Tab should flip between the existing dashboard layout and a read-only explorer view showing the project files and preview -- same explorer from startup but rendered in the Running phase middle section instead of the agent output + task queue. This lets users browse and review files while the pipeline runs without stopping it. [-PID]

## Phase 12: Extension Contracts (Deterministic Context Injection)

- [x] T12.1: Extension contract system -- programmatic enforcement that selected extensions are injected into every agent prompt, not left for the agent to discover. Implementation: (1) Add `extensions: Vec<String>` to .foundry.json config (e.g. `"extensions": ["roblox", "extend"]`). (2) On startup, scan `~/.foundry/extensions/` and project-local `extensions/` for directories containing CLAUDE.md files. Display available extensions on the startup explorer screen as a toggleable list (checkbox UI, persist selection to .foundry.json). (3) In prompts.rs, every prompt function (scout_prompt, planner_prompt, builder_prompt, reviewer_prompt) receives the selected extension contexts. For each selected extension, read its CLAUDE.md and patterns JSON, then prepend the content into the prompt wrapped in a clearly delimited block (e.g. `--- BEGIN EXTENSION CONTRACT: roblox ---`). The agent does not choose to read extensions -- the context is already in its window. (4) Add a prerequisite gate: before IMPLEMENT, verify that all configured extensions have their CLAUDE.md present and non-empty. If an extension is configured but its CLAUDE.md is missing, block the builder with "Extension 'roblox' is configured but CLAUDE.md not found at ~/.foundry/extensions/roblox/CLAUDE.md". (5) Extension patterns (e.g. roblox-common-issues.json) are loaded and merged into the pattern matching pool automatically when the extension is selected -- no manual merge needed. This ensures domain-specific patterns are always active for the selected domain. The key architectural principle: extension context is a CONTRACT enforced by the pipeline, not a suggestion the agent may or may not follow. Prompt-based "please read this file" has a non-zero failure rate. Programmatic injection has zero failure rate. [-PID]

- [x] T12.2: Extension manager TUI -- Visible on BOTH screens (explorer and dashboard). Explorer screen: add an Extensions panel between the file explorer and the input prompt, showing available extensions as a toggleable checkbox list. Each row shows: [x] name, one-line domain description (from first line of CLAUDE.md after the heading), and pattern count. Toggle with Enter or click. Selection persists to .foundry.json. The status bar at the bottom of BOTH the explorer and dashboard screens always shows: "Extensions: roblox (1 active)" or "Extensions: none". This is the contract indicator -- users always know what extensions are active. During Running phase, the extensions line in the status bar is read-only (can't toggle mid-pipeline). On the startup explorer screen, it's interactive (can toggle before starting). Layout: the extensions panel renders below the file explorer left column, sharing the 36% width. If no extensions are found in ~/.foundry/extensions/ or project-local extensions/, the panel shows "No extensions found" with a hint about where to add them. [-PID]

## Roadmap

- [x] R1.1: Auto-create PR after task queue completion. When all tasks are done (or on stop), optionally create a GitHub PR with a summary of all completed tasks. One PR per session, not per task. Config: `auto_create_pr: true`.
- [x] R1.2: Per-task PR creation for team workflows. Each `feat(T1.1)` commit gets its own PR on a feature branch. Config: `pr_per_task: true`. Heavier, but matches team code review workflows. [S-ID]
- [x] R1.3: Auto-create GitHub issue on WIP commit. When verify fails and a task gets `WIP(T1.1)`, auto-create a GitHub issue with the review findings so failed tasks don't sit silently in TASKS.md. Config: `create_issue_on_wip: true`. [-PID]
- [x] R1.4: Explorer toggle controls -- expand/collapse all and word wrap. Add two clickable toggle buttons to the startup and running explorer screens. (1) File explorer pane: render a small clickable `[+all]`/`[-all]` toggle in the top-right corner of the Files box border. Clicking `[+all]` expands every directory in the file tree (set expanded=true on all is_dir entries). Clicking `[-all]` collapses them all (set expanded=false). After toggling, recalculate visible_indices and clamp explorer_selected/explorer_scroll to valid positions. (2) Preview pane: render a clickable `[wrap]`/`[no-wrap]` toggle in the top-right corner of the preview box border. Add a `preview_wrap: bool` field to StartupState (default true). When true, render with `Wrap { trim: false }` (current behavior). When false, render without wrap so long lines are clipped to the pane width -- no horizontal scroll needed, just truncation. (3) Both toggles are mouse-clickable via hit-test on their rendered position (top-right corner of each pane's border area). Also wire keyboard shortcuts: 'w' toggles wrap when Preview is focused, 'a' toggles expand-all when Explorer is focused. (4) Both toggles work identically on the startup explorer and the running-phase explorer (Tab view). Persist wrap preference to .foundry.json as `preview_wrap: bool`. [SPID]
- [x] R1.5: Git status indicator and PR mode guard. Two related changes: (1) Show Git initialization status on both the startup header and the dashboard Stats panel. On startup, detect whether the project directory is a git repo (`git rev-parse --is-inside-work-tree`), whether a remote is configured (`git remote`), and the current branch. Display a compact indicator in the Stats panel: "Git: main | origin | 13 dirty" when initialized, or "Git: not initialized" in red when no repo is found. This is critical because PR creation features (R1.1, R1.2, R1.3) silently fail without git. The startup header line already shows "Git: branch main | N dirty" -- extend it to also show remote name and warn if no remote exists (since PRs need a remote). (2) Guard PR creation to only trigger in Review (hil) mode, not Auto mode. Currently `auto_create_pr` and `pr_per_task` fire regardless of run_mode. In Auto mode the pipeline keeps going -- it should commit but NOT create PRs, since there's no human to review/approve them. Add a check in the PR creation paths (git.rs `commit_and_push`, `commit_task_pr`, and the session-end PR in build.rs) that skips PR creation when `run_mode != "hil"`. Log a message when skipping: "PR creation skipped (auto mode -- switch to Review mode for PRs)". In Review mode, the pipeline already pauses for human approval, so PRs fit naturally into that flow. [-PID]
- [x] R1.6: Extension and pattern usage telemetry. Track and surface how extensions and patterns are actually used during builds so problems (e.g. extension configured but never referenced by an agent) are visible. Implementation: (1) When an extension contract is injected into an agent prompt, log an ExtensionInjected event with the extension name, agent role, and task ID. When the agent's output references extension-specific concepts (e.g. mentions keywords from the extension's CLAUDE.md like "CFrame", "orchestration", "iDRAC"), log an ExtensionReferenced event with the extension name, a short description of what was referenced (e.g. "used CFrame positioning rule", "applied orchestration retry pattern"), and the agent role. (2) Same for patterns: when a pattern is injected, log PatternInjected; when agent output references the pattern's solution or keywords, log PatternApplied. (3) Add counters to AppState: `extension_inject_count: HashMap<String, usize>`, `extension_reference_count: HashMap<String, usize>`, `pattern_inject_count: usize`, `pattern_apply_count: usize`. (4) Dashboard stats panel: show "Extensions: roblox 4/4 injected, 3 referenced" and "Patterns: 5 injected, 2 applied" alongside existing git/pattern stats. (5) Status bar during running: update the existing "Ext: roblox (1 active)" to show usage, e.g. "Ext: roblox (4 inj, 2 ref)". (6) On build completion, if an extension was injected into every agent but never referenced, emit a warning: "Extension 'roblox' was injected 4 times but never referenced -- check if the extension content is relevant to this task." This is the diagnostic the user needs to detect silent extension failures. [-PID]

## Discovery Round 26

- [x] D26.1: Per-task PR branch collision and missing annotation. Two bugs in the pr_per_task workflow: (1) commit_task_pr in git.rs:377 uses `git checkout -b` which fails if branch `foundry/{task_id}` already exists from a previous failed run or retry of the same task -- the bail propagates to build.rs:1371 fallback which silently commits to the current branch instead of creating a PR, losing the per-task PR workflow without a clear diagnostic. Fix: use `git checkout -B` (force-create) to overwrite stale branches, or delete-then-create. (2) When pr_per_task creates a PR in build.rs:1355-1378, the PR number is logged but never written back to TASKS.md via annotate_tasks_with_pr (the session-end PR path at build.rs:641 does annotate). Individual tasks cannot be traced to their PRs in TASKS.md. Add annotate_tasks_with_pr call after successful per-task PR creation, then commit the annotation to the feature branch before switching back to base. [-PID]
- [x] D26.2: Stats panel display bugs -- four issues in the dashboard during running phase. (1) Extension telemetry format at stats.rs:111 has duplicated inj value: `format!("{} {}/{} inj, {} ref", name, inj, inj, refs)` outputs "recon 3/3 inj, 2 ref" with inj shown twice via {}/{} -- the second positional arg should be removed (just "{} {} inj, {} ref"). (2) Dashboard feat/WIP commit counters (session_feat_commits, session_wip_commits) are only populated from git log when entering the startup surface at startup.rs:970-983; during running phase the TaskCompleted handler at app.rs:666-687 does not increment them (comment says "refreshed when returning to startup"), so counters show stale startup values for the entire session even as commits happen. Increment in TaskCompleted based on the success bool. (3) role_configs() at config.rs:316-325 omits Reviewer and Fixer from the Pipeline config table, even though both have configurable provider/model (reviewer_provider, reviewer_model, fixer_provider, fixer_model) in .foundry.json -- users cannot see their review/fix model configuration on the dashboard. Add both roles to role_configs(). (4) Docstring on save_preview_wrap at config.rs:293 says "Persist the selected extensions list" -- copy-pasted from save_extensions, should say "Persist the preview wrap preference". [-PID]

## Discovery Round 27


- [x] D27.2: Unified three-mode run system (Auto / Sprint / Review). Expand the current two-state `run_mode` ("auto" vs "hil") into three values: `"auto"` (default, current behavior), `"sprint"` (new), and `"review"` (replaces "hil"). This is the central execution mode for the entire pipeline. (1) **Config**: Add `"sprint"` as a third valid value for `run_mode`. Keep `"auto"` as default. Normalize `"hil"` to `"review"` on load for backward compat. Remove the separate `pr_per_task` flag -- PR-per-task behavior is now implicit in Review mode. (2) **Build loop behavior per mode**: **Auto** (current): run all tasks, commit each with `feat()`/`WIP()`, when queue empties run the discovery agent to find new work, repeat forever. No PRs created. The pipeline stage shows "DISCOVER" (green). **Sprint**: run all tasks, commit each with `feat()`/`WIP()`, when queue empties send `LoopEvent::Finished` and stop. No discovery, no PRs. Log "Sprint complete -- all N tasks done". The pipeline stage shows "DISCOVER" (dimmed/gray, won't activate). **Review**: run one task at a time. After each task completes (commit + push), create a PR via `commit_task_pr`. Then PAUSE the loop -- set a new `AppState.awaiting_review: bool = true` flag and log "Waiting for PR review -- press Enter to continue to next task". The loop blocks on a channel or checks the flag each tick. When the user presses Enter (or 'c' for continue), clear the flag and proceed to the next task. When queue empties in Review mode, stop (no discovery). The pipeline stage shows "DISCOVER" (dimmed/gray, won't activate). (3) **Startup toggle**: The existing `Ctrl+M` toggle currently cycles between Auto and Review. Expand it to a three-way cycle: Auto -> Sprint -> Review -> Auto. The header badge (already showing "Auto" or "Review") now also shows "Sprint". The `^M` status bar hint updates to show the NEXT mode: `^M sprint` when in Auto, `^M review` when in Sprint, `^M auto` when in Review. Persist to .foundry.json as `run_mode`. (4) **Pipeline visualization**: Rename the disconnected "LOOP" stage in pipeline.rs to "DISCOVER". In Auto mode: show "DISCOVER" with green border when the discovery agent is active, dim when not yet used. In Sprint and Review modes: show "DISCOVER" with gray border and dimmed text (it won't activate). (5) **Remove old flags**: Remove `pr_per_task` and `auto_create_pr` from Config -- these are subsumed by the mode. Auto: commit only, no PRs. Sprint: commit only, no PRs. Review: commit + PR per task, pause for review. Keep `create_issue_on_wip` as an independent opt-in flag (useful in any mode). (6) **Header layout**: Keep the current header structure: `context-foundry  STOPPED  Auto  Explore`. The mode badge position between STOPPED/RUNNING and the tab name stays the same. Just add "Sprint" as a third possible value. Both Explore and Dashboard screens already show this consistently (fixed earlier this session). [SPID]
- [x] D27.3: Auto-resume on PR approval in Review mode. When in Review mode, instead of requiring the user to manually press Enter to continue after each PR, poll GitHub for the PR review decision and auto-resume when approved. Implementation: (1) After creating a PR in Review mode, store the PR number in AppState (e.g. `awaiting_pr: Option<u64>`). (2) Spawn a background polling task that runs `gh pr view <number> --json reviewDecision,state` every 30 seconds. Parse the JSON: if `reviewDecision == "APPROVED"` or `state == "MERGED"`, send a new `LoopEvent::PrApproved` event to the TUI channel. (3) On `PrApproved`, clear `awaiting_review` and resume the loop to the next task. Log "PR #N approved -- resuming pipeline". (4) If the PR is closed without merge (`state == "CLOSED"`), log a warning and stop the loop: "PR #N was closed without merge -- stopping". (5) Show polling status in the header or status bar while waiting: "Waiting for PR #42 review... (last checked 15s ago)". (6) The user can still press Enter to skip waiting and force-continue, overriding the poll. (7) Configurable poll interval via `pr_poll_interval_secs` in .foundry.json (default 30). (8) Requires `gh` CLI to be installed and authenticated -- detect on startup and warn if missing when Review mode is selected. [-PID]
- [x] D27.4: Stats panel commit counters show all-time totals instead of session counts, and Pipeline table role ordering is wrong. Two display issues: (1) refresh_git_commit_counts at startup.rs:964-985 scans the entire git log and sets session_feat_commits/session_wip_commits to the total historical count of feat/WIP commits. Then TaskCompleted at app.rs:668-672 (added in D26.2) increments further during runtime. The counter is named session_* and displayed as "feat: N" in the Stats panel (stats.rs:136-139), implying session scope, but actually shows all-time repo totals plus runtime delta. Fix: either (a) initialize the counters to 0 in AppState::new and only count runtime commits (true session metric), removing the refresh_git_commit_counts initialization, or (b) rename the fields and relabel the Stats display to "total" so the semantics match the values. (2) role_configs() at config.rs:316-327 lists Discovery between Implement and Reviewer/Fixer, but the actual pipeline order is Scout -> Plan -> Implement -> Review -> Fix -> Discovery -> Patterns. The Pipeline config table is misleading. Fix: reorder to Scout, Plan, Implement, Reviewer, Fixer, Discovery, Patterns, Add Tasks. [-PID]
- [x] D27.1: Per-task PR workflow causes TASKS.md revert and infinite loop -- three related bugs in the pr_per_task code path. (1) CRITICAL: When commit_task_pr (git.rs:359-503) commits changes to the feature branch and switches back to base via git checkout at git.rs:498-501, TASKS.md reverts to the base branch version. The mark_done and update_task_progress writes at build.rs:1349-1352 are only captured in the feature branch commit, so after switching back the task still appears as pending (- [x] instead of - [x]). The build loop re-parses TASKS.md at build.rs:487 and re-processes the same task, causing an infinite loop. Fix: after switching back to base, re-apply the TASKS.md modifications (mark_done + progress indicator) to the working directory on the base branch, then commit them to base so the loop sees the task as completed. (2) annotate_tasks_with_pr (git.rs:574-602) iterates ALL lines and adds PR:#N to every - [x] line that lacks a PR tag. When called from commit_task_pr at git.rs:479 for task T3.1, it incorrectly tags T1.1, T2.1, etc. with T3.1s PR number. The session-end PR path at build.rs:641 correctly annotates all tasks (the PR covers them all), but the per-task path should scope annotation to only the current task_id. Fix: add a task_id filter parameter so per-task annotation only modifies the matching task line. (3) The session-end PR block at build.rs:618 has no guard for pr_per_task mode. When both mode=="hil" and pr_per_task==true, individual tasks already have their own PRs, so the session-end PR is redundant (empty diff or duplicate). Fix: skip the session-end PR when pr_per_task is enabled, or only create it if WIP commits exist that were not covered by per-task PRs. [-PID]

## Discovery Round 28

- [x] D28.1: Review mode commit robustness -- three bugs in the Review mode commit/PR workflow. (1) BUG: commit_task_pr at git.rs:391 uses checkout -B (force-create local branch) but the push at git.rs:456 uses plain push without --force. If the remote branch foundry/{task_id} already exists from a previous failed run or retry, the push fails silently (divergent history). The code at git.rs:462 skips PR creation when push fails, so the commit is stranded on the local feature branch while TASKS.md on the base branch says the task is complete (via the re-apply at git.rs:520-538). The user loses code changes without a clear diagnostic. Fix: use --force-with-lease on push to handle stale remote branches, or detect push failure and fall back to merging into base with a clear log message. (2) BUG: When validation fails in Review mode (validated=false), the condition at build.rs:1379 (run_mode == "review" && validated) is false, so the code falls through to the else branch at build.rs:1458 which calls regular commit_and_push. This commits WIP code directly to the base branch, bypassing feature branch isolation entirely. In a team workflow using Review mode for code review gates, WIP code polluting the base branch undermines the purpose of the mode. Fix: use commit_task_pr for WIP commits in review mode too (create the branch and commit, but skip PR creation since it failed validation), or at minimum log a warning that WIP is going to base. (3) BUG: Running header badge background color at tui/running.rs:106-114 is always Green when not Startup and not stop_after_task. The badge text correctly shows "PAUSED (Review)" or "POLLING PR" when awaiting_review is true (lines 97-102), but the background color condition at lines 108-114 does not check awaiting_review, so the badge appears as green "PAUSED (Review)" instead of yellow. Fix: add an awaiting_review check to the bg color conditional to use Yellow when paused or polling. [-PID]
- [x] D28.2: Async correctness and dead code cleanup in Review mode polling. (1) BUG: poll_pr_review at build.rs:310-360 uses std::process::Command (blocking I/O) inside an async fn running on a tokio::spawn task. The gh pr view command involves a network round-trip that can take seconds, blocking a tokio worker thread for the entire duration. With the default tokio thread pool (equal to CPU count), blocking even one thread reduces async throughput. The same build.rs file already imports tokio but poll_pr_review uses std::process::Command. Fix: replace std::process::Command with tokio::process::Command and .await the output, or wrap the blocking call in tokio::task::spawn_blocking. (2) CLEANUP: create_pr (git.rs:237-331) and detect_default_branch (git.rs:336-367) are dead code after D27.2 removed the HIL batch-PR block. They are annotated with #[allow(dead_code)] rather than deleted. commit_task_pr (which IS used) has its own inline PR creation logic. Remove both functions and their dead_code annotations to reduce maintenance surface. [-PID]

## Discovery Round 29

- [x] D29.1: Review mode feature branch isolation is incomplete -- early-exit WIP commits bypass commit_task_pr, create_issue_on_wip is unreachable in review mode, and headless review mode creates orphaned PRs. (1) BUG: D28.1 fixed the final commit path at build.rs:1380 to use commit_task_pr for both validated and WIP commits in review mode. However, 6 early-exit paths in process_task still call git::commit_and_push() directly, committing WIP to the base branch regardless of run_mode: planner failure (build.rs:1091), stop after planner (build.rs:1123), extension contract validation failure (build.rs:1151), planner gate retry failure (build.rs:1227), builder failure (build.rs:1295), and stop after builder (build.rs:1314). In review mode these should use commit_task_pr to maintain feature branch isolation. Additionally, the commit_task_pr error handler at build.rs:1438 falls back to commit_and_push, which also bypasses PR isolation. Fix: wrap each early-exit commit_and_push call in a run_mode check and use commit_task_pr when in review mode. (2) BUG: The create_issue_on_wip feature only fires in the auto/sprint commit path at build.rs:1477-1500 (inside the else block at line 1460). In review mode, WIP commits go through the commit_task_pr path at build.rs:1382, which has no create_wip_issue call. Users who enable create_issue_on_wip and use review mode get no GitHub issues for failed validations. Fix: add the create_wip_issue call to the review mode path after a WIP commit_task_pr completes. (3) BUG: In headless mode (commands.rs:222-226), WaitingForReview auto-clears the review gate so the build loop does not hang. But commit_task_pr already ran and created a PR on GitHub. The PR sits open with no polling (the spawned poll task exits immediately because the gate is already cleared). Users running foundry run (headless) with run_mode review get N orphaned open PRs on GitHub that are never monitored for approval. Fix: either skip PR creation in headless review mode (use commit_task_pr with is_wip=true to push the branch without creating a PR), or log a clear warning that review mode is not supported in headless mode and fall back to auto mode behavior. [-PID]

## Discovery Round 30

- [x] D30.1: Review mode commit ordering race and silent failure handling -- four bugs and one test gap in the review gate and commit paths. (1) BUG (race condition): At build.rs:1441-1442 and build.rs:1487-1488, review_gate.store(true, Relaxed) is called AFTER tx.send(WaitingForReview). If the TUI event handler receives WaitingForReview (setting state.awaiting_review=true at app.rs:813) and the user presses Enter before the build loop sets the gate, the gate.store(false) in the key handler (app.rs:879) is a no-op because the gate is still false. Then the build loop sets it to true, and the while loop at build.rs:1460 hangs forever with no way to clear the gate. Fix: swap the order at both locations -- set review_gate.store(true) BEFORE tx.send(WaitingForReview) so the gate is armed before any handler can try to clear it. (2) BUG (silent failure): commit_wip_for_mode at build.rs:858-873 calls commit_task_pr and uses .unwrap_or(false) on the Result with no logging. If commit_task_pr fails (e.g., git checkout -B fails, or disk full), all 6 early-exit callers silently lose the WIP changes with zero diagnostic output. In contrast, the main commit path at build.rs:1478-1502 logs "WARNING: Per-task PR failed: {e} -- falling back to base-branch commit". Fix: log an error message before the unwrap_or fallback, matching the error handling pattern of the main commit path. (3) BUG (annotation push inconsistency): The initial push in commit_task_pr at git.rs:326 uses --force-with-lease (fixed in D28.1), but the annotation push at git.rs:381-384 uses plain push without --force-with-lease. Both target the same feature branch. If the initial push succeeds via force-with-lease (overwriting a stale remote branch), the annotation push can fail because the remote now has a non-fast-forward history from the annotation's perspective relative to the prior stale state. Fix: add --force-with-lease to the annotation push at git.rs:382. (4) CLEANUP: gather_git_context (git.rs:89) and GitContext struct (git.rs:8) have stale #[allow(dead_code)] annotations but both are actively used from startup.rs:50. These mask real dead code warnings from clippy. Fix: remove both annotations. (5) GAP: Zero test coverage for the is_wip=true path in commit_task_pr. All 3 existing test call sites (git.rs lines 657, 701, 887) pass is_wip=false. The WIP path uses a different commit message prefix (WIP vs feat), different commit body, and skips PR creation -- none of which is tested. Fix: add a test that calls commit_task_pr with is_wip=true and verifies the WIP commit message prefix, that no PR creation is attempted, and that the feature branch exists with the WIP commit. [-PID]

## Discovery Round 31

- [x] D31.1: Review mode commit fallback, config persistence, and PR polling reliability -- four bugs across the review mode subsystem. (1) BUG: commit_wip_for_mode at build.rs:858-879 does not fall back to commit_and_push when commit_task_pr returns Err. D30.1 added warning logging but not the fallback. The main commit path at build.rs:1484-1488 has exactly this pattern: on Err, log a warning and call commit_and_push to preserve changes on the base branch. Without this fallback, all 6 early-exit WIP callers (planner failure at :1117, stop-after-planner at :1147, extension gate at :1175, planner-retry-gate at :1251, builder failure at :1319, stop-after-builder at :1338) silently lose changes when commit_task_pr fails in review mode (git checkout failure, disk full, permission error). Fix: add the same fallback pattern from the main path -- on commit_task_pr Err, call git::commit_and_push as a last resort and log that feature branch isolation was bypassed. (2) BUG: save_run_mode at config.rs:322-332 adds "run_mode" to .foundry.json but does not remove the legacy "mode" key when it exists. Config has #[serde(alias = "mode")] so both keys deserialize to the same field, with the last key in the JSON object winning. When a user upgrades from an old config with "mode": "loop", toggling via Ctrl+M produces {"mode": "loop", "run_mode": "sprint"}. If the JSON is ever reordered (manual edit, different serialization library), the legacy "mode" key shadows the saved value. Fix: in save_run_mode, after setting value["run_mode"], also call value.as_object_mut().map(|o| o.remove("mode")) to clean up the legacy key. (3) BUG: poll_pr_review at build.rs:317-318 sleeps pr_poll_interval_secs BEFORE the first poll. The loop body is: sleep -> gate_check -> gh_pr_view -> process. This means after creating a PR, the user always waits the full interval (default 30s) before the first check. If the PR was approved by a bot or fast reviewer during that window, the delay is unnecessary. Fix: restructure the loop to gate_check -> gh_pr_view -> process -> sleep, so the first poll happens immediately after spawning. (4) BUG: poll_pr_review at build.rs:338-361 does not surface the CHANGES_REQUESTED reviewDecision. When a GitHub reviewer requests changes, the poller silently continues with no user feedback. The TUI shows "POLLING PR" and "last checked Xs ago" but gives no indication that the PR was reviewed and needs changes. The user has no way to know they need to act without checking GitHub directly. Fix: when reviewDecision is "CHANGES_REQUESTED", send a BackgroundLog event like "PR #N: changes requested -- update the code or press Enter to skip" so the status appears in the TUI log. [-PID]

## Discovery Round 32

- [x] D32.2: Improve SPID context window % accuracy by tracking per-turn token counts instead of estimating from cumulative totals. Currently agent.rs parse_claude_result_event uses cumulative input tokens (cache_creation + cache_read + input) divided by num_turns * 2 to approximate the final turn's context usage. This heuristic breaks down for sessions with many short turns (underestimates) or few long turns (overestimates). Fix: track per-turn input token counts from the streaming assistant/user JSON events in read_pty_output, and use the last turn's actual input tokens for the context % calculation instead of the cumulative estimate. [SPID]
- [x] D32.3: Add TUI color theme support -- configurable via `theme` in `.foundry.json` (e.g., "dark", "catppuccin", "solarized"). Define theme as a struct mapping semantic roles (accent, surface, text, muted, success, warning, error) to ratatui Color values. Replace hardcoded Color:: references in tui/ with theme lookups. Ship 2-3 built-in themes. Port the theme selector from Studio if reusable. [SPID]
- [x] D32.4: Add dual-model execution to the build pipeline -- allow configuring two builder models (e.g., `builder_models: ["opus", "codex"]`) that run the same implementation task in parallel. This absorbs Studio's primary unique feature into the main pipeline. UI approach: no new screens or split panes. Reuse the existing agent output pane with tab/number-key switching between streams (e.g., `1: Opus (47 events)` / `2: Codex (32 events)`). Stats row shows both context windows side by side (e.g., `I: 42%/67%`). Pipeline diagram shows both model names on the active stage (`IMPLEMENT (Opus + Codex)`). When both finish, a comparison summary replaces the agent pane: diff size, test results, which output the reviewer picked. Design decisions to resolve during planning: how to compare outputs (diff size, test pass rate, review score), whether to run in parallel or sequential with fallback, how to handle one finishing before the other, and whether the reviewer sees both diffs or just the winner. [SPID]
- [x] D32.1: Review mode PR polling dedup, existing-PR detection, and base branch push gap -- three bugs in the review mode PR subsystem. (1) BUG (log spam): poll_pr_review at build.rs:350-357 sends a BackgroundLog "PR #N: changes requested -- update the code or press Enter to skip" on EVERY poll cycle (default 30s) when reviewDecision is CHANGES_REQUESTED. D31.1 added the CHANGES_REQUESTED handling but acknowledged this gap. Over a typical review cycle (30+ minutes), this generates 60+ duplicate log entries that obscure other useful messages in the TUI log panel. Fix: track the last-seen reviewDecision in a local variable inside poll_pr_review's loop. Only send the BackgroundLog when the decision changes (e.g., from empty to CHANGES_REQUESTED, or from CHANGES_REQUESTED back to empty after the reviewer re-reviews). (2) BUG (silent failure on retry): commit_task_pr at git.rs:329-354 does not detect when a PR already exists for the feature branch. When retrying a previously failed task in review mode, --force-with-lease push succeeds (git.rs:323-326) but gh pr create fails because "a pull request for branch foundry/xxx already exists". The failure is silently swallowed -- output.status.success() is false at git.rs:347, so pr_number stays None (git.rs:328). Back in build.rs:1473, the poll_handle guard (if let Some(pr_number) = pr_num) is false, so no poller starts. The TUI shows "PAUSED (Review)" with manual-continue-only mode and no diagnostic. The user must check GitHub manually to discover the PR exists. Fix: when push succeeds but gh pr create fails with a non-success exit, attempt gh pr list --head foundry/{task_id} --json number --limit 1 to detect the existing PR and extract its number. Log the reuse: "Existing PR #N found for {task_id} -- reusing". (3) BUG (base branch divergence): commit_task_pr at git.rs:404-413 commits "mark {task_id} done on base branch" locally but never pushes the base branch. In review mode, all task commits route through commit_task_pr which only pushes feature branches (git.rs:323-326 and git.rs:379-382). With auto_push_remote configured, feature branches are pushed but the base branch accumulates local-only "mark done" commits. When PRs are merged on GitHub, the remote base branch gets feature changes. The local base branch has independent "mark done" commits the remote lacks. On next git pull, the user faces divergent TASKS.md histories. Fix: after the base branch "mark done" commit at git.rs:413, call maybe_push_commit(project_dir, config.auto_push_remote.as_deref()) to push the base branch too. [SPID]

## Discovery Round 33

- [x] D33.2: Add Ctrl+D hotkey on the startup screen to toggle dual-model builder mode. [SPID]

## Human Tasks

- [x] H2.1: Create `docs/PITCH.html` -- a single self-contained HTML page that explains Context Foundry to a software architect audience in under 5 minutes of reading. No fluff, no marketing speak, no wall of text. Structure: (1) one-sentence hook at the top ("Context Foundry is a self-hosted build loop that runs AI agents against your codebase and improves them over time"), (2) a tight 3-section layout: What it does (SPID pipeline in plain English, 4 stages, what goes in, what comes out), How it works (agent pipeline + pattern store + doubt loop -- use a simple ASCII or SVG flowchart, nothing elaborate), and Why it matters (pattern learning, dual-model arena, no cloud lock-in). Each section fits on a screen. (3) A minimal config snippet showing .foundry.json with builder_models and theme -- just enough to make it concrete. Style: dark background, monospace body font, muted color palette. Completely self-contained (no CDN, no external assets). No JavaScript. No collapsibles. No 11-section encyclopedias. Target: an architect who hasn't heard of this skims it in 3 minutes and immediately understands what it does and why it's interesting. [SPID!]

- [ ] H3.1: Create a single self-contained `docs/ROUNDUP.html` -- a Texas/western-themed pitch page for Context Foundry aimed at software architects. The page tells the story of Context Foundry in frontier/ranch metaphor language without sacrificing technical precision. Structure and content requirements: (1) MASTHEAD -- big, bold headline in slab-serif style ("Y'all Need a Smarter Build Loop") with a subhead that translates immediately: "Context Foundry is a self-hosted AI agent pipeline that runs your build loop, learns from its mistakes, and gets better every ride." Brief one-paragraph trail boss intro explaining what this thing is for architects who've never heard of it. (2) THE TRAIL DRIVE (SPID Pipeline) -- explain the four pipeline stages Scout, Plan, Implement, Doubt using cattle-drive/frontier metaphors (e.g., Scout rides ahead and maps the territory; Plan draws the route before the herd moves; Implement drives the cattle down the trail; Doubt is the trail boss who inspects every steer at the gate before they enter the pen). Each stage gets: the western metaphor name, what it actually does technically, inputs/outputs, and skip conditions. Use an ASCII or SVG trail-map style flowchart to show the pipeline flow -- styled like a hand-drawn map with waypoint markers. (3) THE RANCH HANDS (Execution Modes) -- a comparison card layout for Loop, Sprint, Review, and Dual modes. Each card uses a ranch-hand character archetype (e.g., Loop = the tireless ranch hand who just keeps going; Sprint = the cowboy who rides hard and stops when the job's done; Review = the foreman who won't let anything ship without a sign-off; Dual = two cowboys racing to see who ropes the steer first). Each card lists: what triggers stage transitions, review gate behavior, commit strategy, and the keyboard shortcut to activate. (4) THE PATTERN BRAND (Pattern Store) -- explain the ~/.foundry/patterns/ store using a cattle-branding metaphor: every solved problem gets branded with a pattern_id so the herd (future builds) can be identified and sorted at the gate. Cover the JSON schema fields (pattern_id, title, severity, keywords, issue, solution), how patterns are matched at plan time, and how merge_project_patterns promotes local brands to the global registry. (5) THE ARENA (Dual-Model) -- the "two cowboys, one job" rodeo section. Explain the dual-model arena: two builder models fork into separate worktrees under .buildloop/arena/, run the same task independently, stream output to labeled tabs in the TUI, and the architect picks the winner. Cover the Ctrl+D 3-state selection (model A only, model B only, both), worktree isolation, and how events are tagged by pipeline index. Use a rodeo/bull-riding frame: both riders out of the chute at the same time, human judge decides who scored. (6) THE DEED (Configuration) -- a two-column table of every .foundry.json field styled like a land deed or title document: field name, type, default, and plain-English description. Cover builder_models, dual_selection, theme, pr_poll_interval_secs, auto_push_remote, and any others. (7) THE POSSE (Extensions) -- brief cards for each extension (Roblox, Workday Extend, Recon, Flowise, Mindcraft) framed as specialist deputies each with their own jurisdiction. One paragraph per extension: domain, key files, and a notable gotcha. (8) TRAIL'S END (Why It Matters) -- closing section that drops the metaphor and speaks plainly to architects: no cloud lock-in, self-hosted, pattern learning compounds over time, doubt loop catches gaps self-review misses, dual-model arena absorbs uncertainty. Style requirements: dark warm background (#1a1008 or similar dark saddle-leather brown), body font system monospace, accent color in burnt orange/saddle tan (#c8621a or #d4821a), success green (#5a9e45), error red (#c0392b), muted gold for headers (#e8b84b). Section headers styled with a lone-star glyph (★) prefix or a Texas star SVG inline. Decorative ASCII border elements (barbed wire, rope knot, boot spur) used sparingly as section dividers -- hand-crafted feel, not cheesy clip-art. Completely self-contained: no CDN, no external fonts, no external images. All SVG and CSS inline. No JavaScript frameworks. The page should be readable by a Texas-based architect in 5 minutes on a laptop over morning coffee and leave them saying "I need to set this up on my ranch." [SPID!]

- [x] H1.1: Create a single self-contained `docs/OVERVIEW.html` file that serves as a comprehensive technical reference for Context Foundry -- structured like a NASA engineering manual: dense, precise, no filler, every section earns its place. The file must be completely self-contained (no external CDN dependencies, all CSS and JS inlined). Cover the following with matching fidelity to the actual codebase: (1) SYSTEM OVERVIEW -- one-paragraph mission statement and a top-level architecture diagram (SVG or ASCII-art rendered in a `<pre>` block) showing the major subsystems: TUI frontend, build pipeline, git integration, dual-model arena, MCP server, extension system, and pattern store. Label every component and every data flow arrow. (2) SPID PIPELINE -- a full swimlane or flowchart diagram (SVG preferred, ASCII acceptable) for the Scout -> Plan -> Implement -> Doubt pipeline. Each stage gets a box listing: inputs consumed, outputs produced, skip conditions, and the files it reads/writes (scout-report.md, current-plan.md, build-claims.md, review-report.md). Show fast-mode and batched-doubt variants as annotated branches on the same diagram. (3) EXECUTION MODES -- a comparison table for Loop / Sprint / Review / Dual modes: how tasks are sourced, how the review gate works, when WIP commits fire, how the PR poller interacts, and what Ctrl+M cycles through. Include the 3-way Ctrl+D dual-selection states (model[0]-only, model[1]-only, both). (4) DUAL-MODEL ARENA -- diagram the worktree forking layout under `.buildloop/arena/{provider}/`, show how events are tagged with pipeline index, how the TUI tab bar switches between streams, and note that no automated winner selection runs -- the human evaluates both worktrees. (5) GIT INTEGRATION -- sequence diagram showing commit_task_pr, the --force-with-lease push, annotation push, existing-PR detection fallback, base-branch "mark done" commit + push, and auto_push_remote guard. Call out the review-mode branch naming convention (`foundry/{task_id}`). (6) TUI LAYOUT -- an annotated wireframe (ASCII box-drawing chars) of the running screen: pipeline diagram row, agent output pane, stats row (context%, tokens, stage timer), dual-stream tab bar, log panel, and keybind footer. Label which AppEvent variant drives each widget. (7) CONFIGURATION REFERENCE -- a two-column table of every `.foundry.json` field: field name, type, default, and one-line effect description. Include builder_models, dual_selection, theme, pr_poll_interval_secs, auto_push_remote, and all others found in config.rs. (8) EXTENSIONS -- one subsection per extension (Roblox, Workday Extend, Recon, Flowise, Mindcraft) with: purpose, key files, critical patterns learned, and gotchas. (9) MCP TOOLS -- table of every tool (read_global_patterns, save_global_patterns, merge_project_patterns, delegate_to_claude_code, search_skills): signature, what it reads/writes, and a one-line usage note. (10) PATTERN STORE -- explain the ~/.foundry/patterns/ JSON schema (pattern_id, title, severity, keywords, issue, solution), how patterns are matched at plan time, and how merge_project_patterns promotes them to global scope. (11) SAMSARA / DISCOVERY -- explain the end-of-tasks scan loop, how Discovery Round N is appended to TASKS.md, and the task ID convention (D<round>.<sub>). Style requirements: dark background (#0d1117), monospace body font (system-ui fallback to Courier), NASA/aerospace color palette (white text, amber accent #f0a500, green for pass #39d353, red for fail #f85149, muted blue for diagrams #58a6ff). Section headers styled as console output with a `>` glyph prefix. Use `<details><summary>` collapsible blocks for long reference tables. No JavaScript frameworks -- plain JS only if needed for tab switching or collapsibles. Print stylesheet included so the doc prints cleanly to PDF at letter size. The finished file should read like it was written by engineers who built the system and are handing it to the next crew going to orbit -- not a marketing one-pager, not an AI-generated wall of text.
