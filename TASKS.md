## Phase 1: Context Foundry Visualizer

- [x] 1.1: Build a single self-contained HTML file at visualizer.html that visualizes the Context Foundry architecture as an interactive diagram. Include all CSS and JS inline -- no external dependencies, no other files. The visualization should show: (1) the build loop pipeline (Planner -> Builder -> Reviewer -> Fixer -> Git Commit) with animated flow between stages, (2) the module map (agent.rs, app.rs, studio.rs, config.rs, prompts.rs, patterns.rs, task.rs, tui.rs, git.rs, utils.rs, update.rs) with clickable nodes that expand to show each module's role, (3) the pattern learning cycle (extract -> store -> match -> inject), and (4) the studio mode as a separate interactive flow (prompt -> scan -> smooth -> run providers -> capture artifacts). Use modern CSS (grid, transitions, animations) and vanilla JS for interactivity. Make it visually polished with a dark theme. Read the actual source files to get accurate descriptions -- do not guess.

## Phase 2: app.rs Refactor (Cross-Model Build Loop)

- [x] T2.1: Extract AppState, AppPhase, AppEvent, LoopEvent, PlanningOutcome, PendingTransition, StartupState, EditorState, and PlanningState into src/app/state.rs. Re-export from app.rs via pub use. All existing code must compile unchanged after the move.
- [x] T2.2: Move the app.rs test block (all #[cfg(test)] tests) into src/app/tests.rs as a child module. Import needed types and functions from sibling modules. All 194 tests must still pass.
- [x] T2.3: Extract startup handlers (handle_startup_event, handle_startup_key, handle_startup_mouse, handle_startup_mouse_at, handle_startup_intent_input, move_startup_selection, set_startup_selected_action, scroll_startup_content, adjust_scroll_offset, selected_startup_action, activate_startup_action, enter_home_surface, enter_startup_surface, enter_startup_surface_for_scenario, detect_startup_scenario, classify_plan_status, and related helpers) into src/app/startup.rs.
- [x] T2.4: Extract editor handlers (handle_editor_event, handle_editor_key, load_editor_state, save_editor) into src/app/editor.rs. Keep the editor inside startup.rs if it is too small to deserve its own file; the builder should judge.

## Phase 3: Polish

- [x] T3.1: Set builder_model to "codex-5.4" and fixer_model to "codex-5.4" in the Default impl for Config (config.rs). The TUI agent status line shows BUILDER () with empty parens because Codex roles have an empty model string. After this change the TUI should show BUILDER (codex-5.4) instead. Update any tests that assert on default model values.

- [x] T3.2: Add a hot-inject input to the Running phase TUI. When the user presses 'i' during the build loop, show a single-line text input overlaid on the log pane. On Enter, append the text as a new task line (- [ ] H1.1: <user text>) to TASKS.md and dismiss the overlay. On Esc, cancel without writing. The loop already re-parses TASKS.md before each task, so the injected task will be picked up at the next task boundary. Do not use a mouse target for this -- keyboard only to avoid accidental activation.

- [x] T3.3: Move the "What This Drives" flow diagram from the bottom-right content area to directly below the Actions list on the left-hand side of the startup screen. Currently the flow diagram (showing SPEC.md -> TASKS.md -> build loop) renders inside startup_content_sections as a sub-area beneath the preview. It should instead render as a small block underneath the Actions list in the left column of startup_layout, so the user sees the actions and the flow context together without looking across to the right pane. The right pane should be fully dedicated to the preview content.

- [x] T3.4: Add a real-time dashboard view to the Running phase TUI, accessible via the 'd' hotkey (toggles between the current agent-output view and the dashboard; press 'd' again to return). The dashboard should include: (1) A hero section showing a subway-map style diagram of the build loop pipeline (Planner -> Builder -> Reviewer -> Fixer -> Git Commit -> next task or Discovery), rendered with box-drawing characters, where the currently active stage pulses or highlights with a distinct color and the completed stages dim. Show the provider name on each stage (e.g. "Planner (Claude opus)" / "Builder (Codex)"). (2) A progress summary: tasks completed vs total from TASKS.md, shown as a filled/empty bar and percentage; list of recently completed task IDs with pass/WIP status; the current task description; the next queued task. (3) Git stats: number of commits made this session (count feat() and WIP() separately), dirty file count. (4) Pattern stats: total patterns in the global patterns directory, patterns matched for the current task, patterns learned this session. (5) Provider health: show Claude and Codex with a connected/disconnected/rate-limited indicator based on the last agent exit status for each provider. (6) Session config: project directory, planner/builder/reviewer/fixer/discovery provider+model pairs from .foundry.json, timeout, pause settings. (7) Timing: elapsed time for current task, elapsed time for current agent, total session runtime. Use the existing TUI color scheme. The dashboard reads from AppState fields that the loop already maintains (completed_count, total_count, current_task, current_agent, log_messages, etc.) plus a few new counters (session commit counts, pattern stats) that should be added to AppState. Render with ratatui widgets (Gauge for progress, Table for config, Paragraph for the loop diagram). This is a read-only view -- no input handling beyond 'd' to toggle back.

- [x] T3.5: Add automatic provider fallback to the build loop. When a Codex agent fails with a rate-limit or subscription-limit error (detect by checking AgentExitKind and the failure_message for rate/limit keywords), the loop should retry the same agent step using Claude as the fallback provider instead of failing the task. Implement this in run_agent() in agent.rs: after the Codex retry loop exhausts its attempts or detects a rate-limit failure, construct a new run_agent call with ModelProvider::Claude and the reviewer_model (opus) as the fallback. Log the fallback clearly (e.g. "Codex rate-limited, falling back to Claude opus"). This should be transparent to the build loop -- build.rs and review.rs do not need to change.

## Discovery Round 1

- [x] D1.1: Fix unchecked indexing panic in studio/state.rs:656 -- selected_execution_contract() uses direct array index which panics if execution_contracts is empty or index is stale; use .get() with expect or return Result, and add bounds validation in set_selected_execution_contract_index() at line 660
- [x] D1.2: Replace hardcoded 'push origin main' in git.rs:111 with dynamic branch/remote detection -- use 'git symbolic-ref --short HEAD' for current branch and 'git remote' to detect the configured remote, preventing silent push failures for non-main branches or missing origin
- [x] D1.3: Replace .expect() on mutex locks in agent.rs (lines 178, 195, 337) with unwrap_or_else(|p| p.into_inner()) to prevent cascade panics when a thread panics while holding the provider progress lock


## Phase 5: Doubt Loop TUI Integration

- [x] T5.1: Add a "Design with review" startup action (available in all scenarios except EmptyProject). When selected, open the intent input. On Enter, run the orchestrator loop (src/orchestrator.rs) through the TUI Planning phase -- showing proposer/reviewer iterations in the agent output panel, iteration count and finding count in the header, and the final artifact in a preview on completion. Return to startup with the output in .buildloop/orchestrator-output.md. Do not auto-write to TASKS.md or SPEC.md.

- [x] T5.2: Update the Planning phase header to show the orchestrator iteration count and role (e.g. "Iteration 2/3: Reviewing with Codex codex-5.4" or "Iteration 1/3: Proposing with Claude opus"). The existing planning header shows "Planning -- label" and "PLANNER (model) | time | events". For orchestrator mode, replace "Planning" with "Design" and show which sub-agent (proposer or reviewer) is active. Use a new field in PlanningState to distinguish orchestrator mode from regular planning.

- [x] T5.3: Add a "Review findings" panel to the TUI that appears when the orchestrator completes with unresolved findings. Show the findings list with severity colors (high=red, medium=yellow, low=gray), the validated claims in green, and the iteration history. This panel should be scrollable and accessible via a new key hint in the status bar.

- [x] T5.4: Wire the orchestrator's on_event callback into the TUI event system so proposer/reviewer agent output streams live to the agent output panel during the design loop. Currently the orchestrator uses run_agent_and_capture() which collects output silently. Instead, forward AgentOutputEvent to the TUI's event_tx channel, and add a separator line between proposer and reviewer output (e.g. "--- Reviewer pass ---").

- [x] T5.5: Add the orchestrator config fields (orchestrator_proposer_provider, orchestrator_proposer_model, orchestrator_reviewer_provider, orchestrator_reviewer_model, orchestrator_max_iterations, orchestrator_accept_policy) to the Session Config table on the dashboard view. Show them in a separate "Orchestrator" section below the existing role table.

- [x] T5.6: Add test coverage for the orchestrator TUI integration: startup action creates the right transition, planning header shows orchestrator mode, iteration count updates correctly, findings panel renders severity colors, and agent output forwarding produces visible events.

## Phase 6: Cleanup and Hardening

- [x] T6.1: Remove the header log line duplication during Planning phase. Currently the header shows the planning label on line 2 AND the most recent log message on line 4, which repeats the same "Planning started" text. The log line should be suppressed during Planning phase since the header already provides that context. (Fix is already in tui.rs but needs the build loop to pick it up.)

- [x] T6.2: Add regression tests for the state file backup/restore in build.rs: test that backup captures TASKS.md, SPEC.md, CLAUDE.md, and .buildloop/current-plan.md; test that restore recovers deleted files; test that restore recovers truncated files (under 10 bytes); test that restore does NOT overwrite current-plan.md when it was legitimately rewritten by the planner; test that write failures produce warning log messages.

- [x] T6.3: Add regression tests for the semantic pattern matcher in embeddings.rs: test duplicate pattern_id cache collision does not occur with model:hash keys; test empty patterns list skips Ollama; test circuit breaker cooldown and recovery; test that match_patterns_semantic returns keyword-only results when Ollama is unavailable.

- [x] T6.4: Add regression tests for the orchestrator in orchestrator.rs: test that is_accepted does not trust status="clean" when findings exist; test case-insensitive severity matching; test the full orchestration loop with mock agent responses; test max iterations termination; test JSON extraction from markdown-fenced responses.

- [x] T6.5: Tighten the embeddings.rs stale cache test: instead of just comparing strings, exercise the actual cache load/discard logic by writing a cache file with a wrong model name, loading it, and verifying the stale entry is discarded and re-embedded.

## Discovery Round 2

- [x] D2.1: Fix orchestrator run_agent_and_capture in orchestrator.rs:487 hardcoding timeout to 600 instead of using config.agent_timeout_secs -- pass the configured timeout through OrchestratorConfig so user-configured timeouts are respected
- [x] D2.2: Fix extract_json brace-counting parser in orchestrator.rs:236-255 not skipping braces inside JSON string values -- when artifact_text contains code with braces (e.g. fn main() { }), the depth counter finds the wrong closing brace and produces truncated JSON, causing parse fallback
- [x] D2.3: Pass shutdown token to orchestrator agents in orchestrator.rs:484 instead of None -- currently orchestrator proposer/reviewer agents are uncancellable, unlike build loop agents which all receive the shutdown Arc, so a hung design-with-review agent blocks indefinitely
- [x] D2.4: Narrow should_restart_docker keyword matching in build.rs:562-570 -- the keywords "integration" and "scaffold" are too broad, triggering unnecessary Docker compose down/up cycles for tasks like "Add integration tests" or "Scaffold test fixtures" that have nothing to do with Docker
- [x] D2.5: Add explicit match arm for "no-high-medium" in orchestrator accept_policy parser at orchestrator.rs:69-72 -- the default config value "no-high-medium" currently falls through to the catch-all wildcard arm, which happens to produce correct behavior but is fragile if new arms are added

## Discovery Round 3

- [x] D3.1: Fix race condition in embeddings.rs test test_match_patterns_semantic_returns_keyword_only_when_ollama_unavailable at line 682 -- the test uses force_cooldown() on global static OLLAMA_STATE but test_empty_patterns_skips_ollama (line 650) and test_circuit_breaker_cooldown_and_recovery (line 668) call clear_cooldown() concurrently, causing the cooldown to be cleared before is_available() is checked and returning "keyword-only" instead of "cooldown"; serialize these tests or use a per-test isolation strategy
- [x] D3.2: Fix hardcoded 600-second timeout in run_pattern_extraction at build.rs:519 -- should use ctx.config.agent_timeout_secs instead of literal 600, same class of bug as the orchestrator timeout fixed in D2.1
- [x] D3.3: Fix review loop in review.rs:65-80 not checking reviewer agent success before evaluating the report -- if the reviewer agent crashes without writing review-report.md, parse_audit_findings returns (0,0,0) and the condition verdict_pass || (high == 0 && medium == 0) evaluates to true, silently passing review; add an early return false when the reviewer agent fails

## Discovery Round 4

- [x] D4.1: Fix review loop in review.rs:83-94 silently passing review when reviewer agent succeeds but review-report.md is missing or empty -- parse_audit_findings returns (0,0,0) for missing/empty files and the condition (high == 0 && medium == 0) evaluates to true, bypassing the verdict check; add an explicit check that the report file exists and has content before evaluating findings
- [x] D4.2: Fix orchestrator.rs:424-425 using case-sensitive severity match (== "high") in the finding-count log while is_accepted at line 285 uses eq_ignore_ascii_case -- if the reviewer outputs "HIGH" or "High", the log reports 0 high findings while acceptance correctly rejects, producing misleading output; use eq_ignore_ascii_case consistently
- [x] D4.3: Fix hardcoded 300-second timeout in run_append_tasks at planning.rs:93 -- should use ctx.config.agent_timeout_secs instead of literal 300, same class of bug as D2.1 and D3.2
- [x] D4.4: Cap log_messages growth in state.rs:129 -- the Vec grows without bound during long sessions; the TUI only renders the most recent entry so old entries are invisible but consume memory indefinitely; add a cap (e.g. 500 entries) and drop oldest when exceeded

## Discovery Round 5

- [x] D5.1: Cap agent_output Vec growth in app.rs handle_agent_output -- same class of bug as D4.4 (log_messages); agent_output is pushed to at lines 934, 945, 955, 959, 964 without any bound; during long builder/reviewer runs this accumulates memory indefinitely since the TUI only renders visible lines with scroll offset; add a cap (e.g. 2000 entries) with oldest-drop in handle_agent_output, matching the pattern used for log_messages in state.rs
- [x] D5.2: Fix provider_binary_is_available in commands.rs:46 and studio/providers.rs:571 using Unix-only "which" command -- Windows has no "which" binary so this always returns false on Windows, causing ensure_required_providers_available to reject all providers; the codebase explicitly supports Windows (release.yml builds x86_64-pc-windows-msvc, update.rs has full Windows codepaths); use "where" on Windows or check PATH entries directly
- [x] D5.3: Guard against empty model string in run_agent Claude path at agent.rs:551-552 -- run_provider_session correctly skips --model when model is empty (line 269), but the Claude path in run_agent always passes --model even if model.trim().is_empty(), which sends --model "" to claude CLI; add the same is_empty guard for consistency

## Discovery Round 6

- [x] D6.1: Fix cache_dir() in embeddings.rs:186 and resolve_patterns_dir() in patterns.rs:43 using Unix-only HOME env var for path resolution -- on Windows HOME is unset so cache_dir falls through to /tmp/.foundry/cache (invalid Windows path) and resolve_patterns_dir falls through to /tmp/.foundry/patterns (also invalid); update.rs:67-74 already has the correct pattern using LOCALAPPDATA/USERPROFILE on Windows; apply the same cfg!(target_os = "windows") branching to both functions
- [x] D6.2: Add wrapper-object format support to load_patterns() in patterns.rs:56-81 -- the function only parses flat arrays (Vec<Pattern>) and single objects (Pattern) but not the documented wrapper format ({"pattern_type": "...", "domain": "...", "patterns": [...]}) used by extension pattern files (e.g. extensions/recon/patterns/recon-common-issues.json); if a wrapper-format file is placed in the global patterns directory its patterns are silently skipped; add a serde struct for the wrapper and try parsing it between the array and single-object attempts

## Discovery Round 7

- [x] D7.1: Fix launch_external_editor in app.rs:285-291 using Unix-only "sh -c" to invoke the editor -- on Windows sh does not exist so the editor feature always fails; the codebase explicitly supports Windows (release.yml builds x86_64-pc-windows-msvc, update.rs has Windows codepaths, commands.rs uses "where" on Windows); use cmd /C on Windows or call the editor binary directly without a shell wrapper, matching the platform branching pattern already used elsewhere
- [x] D7.2: Fix discovery_round counter in build.rs:106 starting at 0 every session -- when a new session starts, discovery rounds begin numbering from 1 regardless of existing rounds in TASKS.md, creating duplicate section headers ("## Discovery Round 1") and duplicate task IDs (D1.1, D2.1) that collide with IDs from previous sessions, producing confusing git commits with ambiguous IDs; scan TASKS.md at build_loop initialization for the highest existing discovery round number and start from there
- [x] D7.3: Prune stale entries from the embedding cache in embeddings.rs save_cache at line 227 -- cache entries for patterns that have been deleted or renamed are never removed, causing the cache file (~/.foundry/cache/pattern-embeddings.json) to grow indefinitely over months of use; during save_cache, remove entries whose content_hash does not match any current pattern text

## Discovery Round 8

- [x] D8.1: Add ensure_required_providers_available() call to run_design_command in orchestrator.rs:598 -- all other CLI entry points (run_tui via app.rs:41, run_headless via commands.rs:119, run_plan_mode) validate that required provider CLIs (claude, codex) are installed before spawning agents, but `foundry design` skips this check entirely; when a provider binary is missing, the user gets a cryptic PTY spawn error instead of the clear "required provider CLI not found" message that other commands produce
- [x] D8.2: Cache compiled regexes in task.rs parse_tasks (line 31) and highest_discovery_round (lines 99-100) using std::sync::OnceLock<Regex> -- parse_tasks recompiles Regex::new(r"^([A-Za-z]?\d+\.\d+):\s*") on every call, and the build loop invokes it 4+ times per task cycle (build.rs:109, 159, 222, 249); highest_discovery_round compiles two separate regexes per call; these are hot paths during long sessions with many tasks; use OnceLock or LazyLock to compile once and reuse
- [x] D8.3: Fix double to_lowercase() allocation in check_review_passed at review.rs:294-295 -- content.to_lowercase() is called twice on the same string to check for "verdict: pass" and "verdict:pass", creating two redundant heap allocations of the entire review report; extract to a local variable and reuse

## Discovery Round 9

- [x] D9.1: Add wrapper-format support to merge_patterns() in patterns.rs:216-223 -- when loading existing patterns from common-issues.json for merging, only flat array and single-object JSON formats are tried; the wrapper format ({"pattern_type": "...", "patterns": [...]}) supported by load_patterns() since D6.2 is not handled, so if the file is in wrapper format all existing patterns are silently discarded and replaced by the new ones; use the same PatternWrapper struct already defined at patterns.rs:44
- [x] D9.2: Add stop-file check between agent stages in process_task at build.rs:360-410 -- after the planner completes and before the builder starts (and between builder and reviewer), the function sleeps for pause_between_agents_secs but never checks ctx.stop_file().exists() or ctx.shutdown; if a user touches .buildloop/stop during the planner phase, the entire remaining pipeline (builder + reviewer + fixer) must complete before the stop is honored at build.rs:167; check after each agent stage and return early if stop is requested
- [x] D9.3: Switch run_agent Claude path timeout from wall-clock deadline to idle-based timeout at agent.rs:601 -- the Claude path sets deadline = Instant::now() + Duration::from_secs(timeout_secs) which kills agents that are actively producing output on long tasks; run_provider_session (used by Studio) correctly uses ProviderProgressState.no_progress_for() as an idle timeout with a separate hard deadline multiplier; apply the same idle+hard timeout pattern to the build-loop Claude path for consistency

## Discovery Round 10

- [x] D10.1: Add stop-file/shutdown check to the review loop in review.rs between fixer completion and second reviewer pass -- run_review_loop sleeps at lines 128 and 175 between fixer and the pass-2 reviewer but never checks ctx.stop_file().exists() or ctx.shutdown.load(); same class of bug as D9.2 which added stop checks between stages in build.rs but missed the review loop; if a user touches .buildloop/stop during the fixer or the inter-pass sleep, the entire second reviewer pass must complete before the stop is honored
- [x] D10.2: Sync TUI discovery_round counter with actual round numbers from TASKS.md -- state.rs:182 initializes discovery_round to 0 and app.rs:642 increments on each DiscoveryStarted event, but the build loop at build.rs:107 initializes from highest_discovery_round(); DiscoveryStarted at state.rs:265 carries no round number; the status bar at tui.rs:1753 shows session-relative rounds (1, 2, 3) instead of absolute rounds (e.g. 10, 11, 12) when resuming sessions with existing discovery rounds; either add the round number to the DiscoveryStarted event or initialize the TUI counter from TASKS.md at build loop start

## Discovery Round 11

- [x] D11.1: Add stop-file/shutdown check to the discovery cycle in build.rs:176-257 -- the discovery branch of build_loop never checks ctx.stop_file().exists() or ctx.shutdown.load(); the is_stop_requested() helper is only called inside process_task() and at line 168 after task completion; if all tasks are completed and the loop enters discovery mode, touching .buildloop/stop has no effect until a new task is discovered and fully processed; add stop checks after discovery agent completion (line 221), after the inter-cycle sleep (line 236), and after discovery git commit (line 248); same class of bug as D9.2 and D10.1 which added stop checks in process_task and review_loop but missed the discovery cycle
- [x] D11.2: Pass a shutdown token to run_design_command CLI entry point in orchestrator.rs:618-627 -- the CLI foundry design command passes None for the shutdown parameter to orchestrate(), so PTY-spawned agent subprocesses are not cancellable via the shutdown mechanism; the TUI design path (app.rs:1082) correctly passes Some(shutdown.clone()); without a shutdown token, orchestrate() line 346 short-circuits via is_some_and(false) and run_agent() line 631 does the same, leaving orphan PTY child processes on Ctrl+C since portable-pty creates a new session; create an AtomicBool, install a ctrlc/signal handler to set it, and pass Some(Arc) through to orchestrate()
- [x] D11.3: Make embedding cache write atomic in embeddings.rs save_cache_to at line 247 -- std::fs::write() is non-atomic so the cache file can be truncated if the process is killed mid-write or two foundry sessions write concurrently; patterns.rs merge_patterns already uses the correct pattern (write to .tmp then fs::rename at lines 255-256); apply the same tmp-file-then-rename strategy to save_cache_to for consistency

## Discovery Round 12

- [x] D12.1: Fix background pattern extraction overwriting TUI agent state in build.rs:517-529 -- the fire-and-forget pattern extraction spawned via tokio::spawn sends AgentStarted(Discovery) at line 566 and AgentDone at line 589 through the same event channel (tx) as the build loop; if extraction overlaps with the next task, handle_event at app.rs:614-619 calls set_agent() which clears agent_output, resets scroll_offset, and overwrites current_agent, causing the planner/builder output to vanish and the TUI header to briefly show DISCOVERY; also pushes Discovery into task_stages_seen at line 616 which gets saved to the wrong task's pipeline history at line 631; fix by either suppressing AgentStarted/AgentDone from background extraction or using a distinct background-only log event that does not trigger set_agent
- [x] D12.2: Install a signal handler in run_headless at commands.rs:112-221 to set the shutdown AtomicBool on Ctrl+C -- the headless mode creates a shutdown Arc<AtomicBool> at line 117 but never installs a signal handler to set it; run_agent checks shutdown at agent.rs:631-641 and process_task checks it between stages at build.rs:410,467, but neither fires without a handler; in headless mode Ctrl+C terminates the process abruptly and orphaned PTY child processes may remain running since portable-pty creates a new session; same class of bug as D11.2 which fixed the identical problem for foundry design; add tokio::signal::ctrl_c handler matching the pattern used in orchestrator.rs:618-623

## Discovery Round 13

- [x] D13.1: Install a signal handler in run_plan_mode at planning.rs:131-135 to set the shutdown AtomicBool on Ctrl+C -- same class of bug as D11.2 (foundry design) and D12.2 (foundry run --headless); run_plan_mode creates Arc<AtomicBool>::new(false) at line 135 but never wires it to a signal handler; agents receive Some(ctx.shutdown.clone()) at line 202 via run_gap_analysis_iteration but the flag is never set to true; on Ctrl+C the process terminates abruptly and orphaned PTY child processes may remain; add tokio::signal::ctrl_c handler matching the pattern now used in commands.rs:122-125 and orchestrator.rs:618-623
- [x] D13.2: Replace hardcoded "opus" in Codex-to-Claude fallback at agent.rs:527 with the callers configured Claude model -- when Codex rate-limits and run_agent falls back to Claude, it always uses "opus" regardless of the users .foundry.json configuration; if the user configured a different Claude model for the role (e.g. sonnet for cost control), the fallback ignores it and uses the more expensive model; add a fallback_model parameter to run_agent or resolve the correct role model from the config at the call site in build.rs process_task
- [x] D13.3: Log a warning in Config::load at config.rs:152-153 when .foundry.json exists but fails to read or parse -- both read_to_string and serde_json::from_str use unwrap_or_default() which silently replaces a corrupt or unreadable config with defaults; users get no indication their settings were ignored, making it hard to debug why configuration changes have no effect; add eprintln warnings on read failure and parse failure before falling back to defaults

## Discovery Round 14

- [x] D14.1: Fix race condition between commit_inject_task in app.rs:851-950 and task::mark_done in task.rs:82-91 on TASKS.md -- both functions read-modify-write TASKS.md without coordination; commit_inject_task runs from the TUI event handler (main task) while mark_done runs from the build_loop (spawned tokio task at app.rs:337-339); if a user submits an injected task at the moment the build loop completes a task, one write can overwrite the other, silently losing the injected task or reverting a completed task to unchecked; add a file-level coordination mechanism (e.g. tokio::sync::Mutex around TASKS.md writes, or route inject writes through the build loop via a channel)
- [x] D14.2: Add explicit return false after the pass-2 failure branch in run_review_loop at review.rs:212-223 -- when the second reviewer pass still has high/medium findings, the code at line 218 sends TaskReviewResult with passed=false but does not return; execution falls through to line 226 which calls check_review_passed and re-reads the report file; if the reviewer wrote both high findings in the JSON block and "Verdict: PASS" in the text (inconsistent LLM output), the function returns true while the TUI already recorded false; the fall-through is also wasteful (redundant file read); add return false after line 222 to match the early-return pattern used in all other exit paths of this function
- [x] D14.3: Consolidate duplicate is_stop_requested helper functions in build.rs:299-303 and review.rs:13-17 into a method on RunContext -- both are identical (check stop_file.exists() or shutdown.load()), but maintaining two copies risks divergence if new stop conditions are added; move the logic to a pub method on RunContext in context.rs so build.rs, review.rs, and any future modules use the same implementation

## Discovery Round 15

No new tasks discovered.

## Discovery Round 16

- [x] D16.1: Remove unreachable dead code at review.rs:220 -- after D14.2 added return false at line 216 in the pass-2 failure branch, the check_review_passed call after the for loop (line 220) is unreachable; every path through for pass in 1..=2 returns inside the loop body (pass 1 returns or continues to pass 2, pass 2 always returns); replace line 220 with false or unreachable!("all paths return inside the review loop") to make the control flow explicit
- [x] D16.2: Move "compose" from primary Docker keywords to qualified keywords in should_restart_docker at build.rs:624 -- "compose" is in the primary keyword list that always triggers docker compose down/up, but it is a common English word that appears in non-Docker task descriptions (e.g. "Compose validation error messages", "Compose a response template"); D2.4 narrowed "integration" and "scaffold" to require an infra qualifier but missed "compose"; move "compose" to the broad_keywords list so it only triggers Docker restart when paired with an infra qualifier like "docker", "container", or "service"
- [x] D16.3: Cap task_history HashMap growth in state.rs:138 and app.rs:630,690 -- task_history grows without bound as tasks accumulate across long sessions with many discovery rounds; unlike log_messages (capped at 500 per D4.4) and agent_output (capped at 2000 per D5.1), task_history has no size limit; each entry is small but sessions running hundreds of tasks can accumulate thousands of entries; add a cap (e.g. 200 entries) and evict oldest entries when exceeded

## Discovery Round 17

No new tasks discovered.

## Discovery Round 18

- [x] D18.1: Use atomic write (tmp+rename) for studio file persistence instead of direct fs::write -- history.rs:45 (prompt history JSON), contracts.rs:189 (selected contract), contracts.rs:225-226 (new contract + sidecar), attachments.rs:606 (attachment specs JSON), ui/input.rs:751 (editor choice) all use fs::write() directly; a crash or power loss mid-write can leave partially-written files that fail to parse on next load; this is the same class of bug D11.3 fixed in embeddings.rs; extract a shared atomic_write_file() helper (write to .tmp in same dir, then fs::rename) and use it in these 6 locations; providers.rs:508 and session.rs:244 are best-effort (let _ =) and lower priority but should also be converted for consistency

## Discovery Round 19

- [x] D19.1: Use atomic_write_file from utils.rs for TASKS.md writes in task.rs:90 and app.rs:932,939 -- mark_done() at task.rs:90 uses fs::write() which can corrupt TASKS.md on crash mid-write; commit_inject_task() at app.rs:932 uses fs::write() to rewrite the file and at app.rs:939 uses OpenOptions::append() which is also non-atomic; TASKS.md is the most critical state file in the build loop (losing it means losing all progress tracking); D18.1 fixed the same class of bug in studio files and D11.3 fixed it in embeddings.rs; the atomic_write_file() helper already exists at utils.rs:33; for the append case in commit_inject_task, read-then-atomic-write is safer than non-atomic append
- [x] D19.2: Use atomic_write_file from utils.rs for SPEC.md creation in app.rs:483 and orchestrator output in orchestrator.rs:594 -- seed_spec_from_brief() at app.rs:483 uses std::fs::write() to create SPEC.md from the brief; write_orchestrator_output() at orchestrator.rs:594 uses std::fs::write() for orchestrator-output.md; both are user-visible artifacts where partial writes would produce corrupt files; same class of bug as D19.1 and D18.1; use the existing atomic_write_file() helper
- [x] D19.3: Use atomic_write_file from utils.rs for state file restoration in build.rs:87 -- restore_state_files() uses std::fs::write() to restore backed-up state files (TASKS.md, SPEC.md, CLAUDE.md, current-plan.md); a crash during restore could corrupt the very file being restored, which is ironic since the restore function exists to recover from corruption; the atomic tmp+rename pattern prevents this; the backup read at build.rs:82 is fine but the write at line 87 should use atomic_write_file()

## Discovery Round 20

No new tasks discovered.

## Discovery Round 21

No new tasks discovered.

## Discovery Round 22

- [x] D22.1: Fix UTF-8 panic in inject input display at tui.rs:113-114 -- the slice input[input.len() - max_text..] can panic on a non-char-boundary when inject_input contains multi-byte characters (emoji, CJK, accented chars) because max_text derives from terminal column width (not byte count); app.rs:825-827 accepts arbitrary KeyCode::Char(c) and app.rs:802-804 accepts pasted text so multi-byte input is expected; replace the raw slice with truncate_str_from_end(input, max_text) from utils.rs:21 which already handles char boundary alignment
- [x] D22.2: Guard the Unix-only import in config.rs:213 with cfg(unix) -- use std::os::unix::fs::PermissionsExt is unconditionally imported at the top of the #[cfg(test)] mod tests block, which prevents the entire config test module from compiling on Windows; only the load_warns_on_unreadable_file test at line 274 uses it; move the import inside that test function and add #[cfg(unix)] to the test, matching the pattern used in build.rs:852 and attachments.rs:907-908

## Discovery Round 23

- [x] D23.1: Fix UTF-8 panic in pipeline subway map model label at tui.rs:199 -- display[..12].to_string() slices by byte index which panics if display contains multi-byte UTF-8 characters at the boundary; display is built from provider name and model string at tui.rs:196 which are user-configurable via .foundry.json; same bug class as D22.1 (inject input) but at a different code location; replace with truncate_str(&display, 12) from utils.rs:3 which already handles char boundary alignment
- [x] D23.2: Fix Codex-to-Claude fallback passing Codex-specific model string at agent.rs:528 -- when Codex fails and should_fallback_to_claude() returns true, run_agent() recursively calls itself with ModelProvider::Claude but passes the original model parameter verbatim; if the role is configured with a Codex-specific model name (e.g. the former default "codex-5.4" or any OpenAI model ID), Claude CLI receives an invalid --model argument and the fallback also fails; the fallback should pass an empty string "" to let Claude CLI use its default model, matching T3.5's intent of transparent fallback

## Discovery Round 24

No new tasks discovered.

## Discovery Round 25

- [x] D25.1: Fix UTF-8 slicing panic in parse_reviewer_output fallback at orchestrator.rs:228 -- the expression &response[..response.len().min(200)] slices by byte index which panics if byte 200 falls in the middle of a multi-byte UTF-8 character (emoji, CJK, accented chars); reviewer output can contain arbitrary text so this is reachable; same class of bug as D22.1 (inject input) and D23.1 (pipeline label) but at a different code location; replace with truncate_str(&response, 200) from utils.rs which is already imported at line 12

## Phase 7: Foundry Overview Page

- [x] T7.1: Create foundry-overview.html with a dark-themed layout showing the Context Foundry brand header, a brief tagline, and four main sections: The Build Loop, The Review Gate, The Doubt Loop, and Discovery + Patterns. All CSS and JS inline, no external dependencies. Single file. Read the actual source code to get accurate descriptions.
- [x] T7.2: Add an interactive Build Loop section with clickable stage boxes (Planner > Builder > Reviewer > Fixer > Commit). Clicking a box shows a tooltip explaining what that stage does. Add a simple animation showing flow through the pipeline. Below the pipeline, show the Review Gate: how the reviewer runs up to 2 passes, the fixer runs between passes, and PASS/FAIL verdicts determine whether the commit is feat() or WIP(). This is a loop within the build loop (for pass in 1..=2).
- [x] T7.3: Add a Doubt Loop section showing the Proposer and Reviewer iterating with arrows between them. Show iteration count (up to 3), findings badge with severity colors, and acceptance policy. This is a real loop (for iteration in 1..=max_iterations). Below it, show Discovery (not a loop -- it's what the build loop does when the task queue is empty: scan repo, append new tasks, continue) and Pattern Learning (not a loop -- a pipeline stage that extracts patterns after each task, stores them, and injects matched patterns into future prompts via keyword + semantic similarity).
- [ ] T7.4: Add a Task Queue section showing sample tasks with checkmarks, prefix colors (T=white for planned, D=blue for discovered, H=yellow for human-injected), and the compact PBRF pipeline indicator per task. Make the whole page scrollable and responsive.

## Phase 8: Performance and Efficiency (Issues #199, #201-#206)

- [ ] T8.1: Adaptive inter-agent pauses (#202). In build.rs, track whether the last agent call was rate-limited (the rate_limit_event is already detected and logged). If no rate limit was hit on the previous call, skip the pause_between_agents_secs delay entirely (or use a minimal 500ms delay). If rate limited, keep the configured pause or increase it. Apply the same logic to pause_between_tasks_secs. Add a config field adaptive_pauses (default true). This change touches the sleep() calls at build.rs lines where tokio::time::sleep(pause_between_agents_secs) appears (between planner/builder, builder/reviewer, reviewer/fixer) and the pause_between_tasks_secs sleep after task completion.

- [ ] T8.2: Model routing by task complexity (#203). Add a lightweight task complexity classifier that runs before the planner. Classify tasks as simple/medium/complex based on description length, keyword signals (rename/add/fix vs architect/redesign/implement), and file count hints. Add model tier config to .foundry.json with per-complexity model overrides for planner/builder/reviewer. Simple tasks use faster/cheaper models (sonnet), complex tasks use opus. Default tier config should be sensible without user configuration. The classifier should be a pure function, not an LLM call.

- [ ] T8.3: Skip planner for simple tasks (#204). When the complexity classifier from T8.2 marks a task as "simple", skip the planner stage entirely. Instead of writing current-plan.md and having the builder read it, pass the task description directly to the builder prompt as its specification. Add a builder_direct_prompt() function to prompts.rs that formats the task description as a builder instruction without requiring a plan file. The builder should still run cargo test / npm test as verification. Add a config field skip_planner_for_simple (default true).

- [ ] T8.4: Diff-only review mode (#205). After the builder runs, capture the git diff (git diff HEAD or git diff --cached). Pass the diff inline in the reviewer prompt instead of listing file paths for the reviewer to read individually. For new files (no diff available), include the full content. Add a config field review_mode ("full" or "diff-only", default "diff-only"). Update the reviewer_prompt() function in prompts.rs to accept either a file list or a diff string. The reviewer should still run build/test commands but reads the diff instead of globbing through files.

- [ ] T8.5: Planner look-ahead (#201). While the builder is running task N, spawn the planner for task N+1 in parallel. The look-ahead planner writes to .buildloop/plan-{task_id}.md instead of current-plan.md. When task N completes and task N+1 starts, check if plan-{task_id}.md exists. If it does, rename it to current-plan.md and skip the planning phase. If it doesn't (look-ahead failed or was cancelled), plan normally. Cancel the look-ahead if discovery runs, if the user injects a task, or if the task queue is reordered. Only look ahead by 1 task.

- [ ] T8.6: Smart discovery cooldown (#206). After the task queue empties, check when the last task was added and how it was added. If all completed tasks were from a recent "Describe work" or manual inject (not from a previous discovery), skip the first discovery scan and wait discovery_cooldown_minutes (default 5, configurable in .foundry.json) before scanning. If the previous discovery round found zero new tasks, increase the cooldown exponentially (5 min, 15 min, 30 min) up to a max. Reset the cooldown when the user adds tasks manually.

- [ ] T8.7: Parallel task execution for independent tasks (#199). This is the largest change. Add a dependency analyzer that groups pending tasks into parallel batches. Tasks within the same phase section that don't reference each other's output files can run in parallel. For each parallel batch: fork the repo into N git worktrees (one per task), run each task's full pipeline (planner > builder > reviewer > fixer) concurrently, then merge worktrees back sequentially with conflict detection. Update the TUI to show multiple active tasks with multiplexed agent output. Add a config field max_parallel_tasks (default 3). This task depends on T8.1-T8.6 being stable first.
