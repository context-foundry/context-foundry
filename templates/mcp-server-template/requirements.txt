# MCP Server Requirements
# Install with: pip install -r requirements.txt

# Core MCP framework
fastmcp>=2.0.0

# Optional: For async HTTP requests (used in fetch_data example)
httpx>=0.24.0

# Optional: For environment variables
python-dotenv>=1.0.0
