TEST PHASE INSTRUCTIONS

**Step 1: Read Test Plan**

Read the architecture provided in your context (or `.context-foundry/architecture.md` if not injected).
Find the "Testing Requirements" section.

**Step 2: Run Tests**

Use `/agents` for test execution:
```
Type: /agents
Description: "Expert QA engineer who runs comprehensive tests, analyzes failures, and creates detailed test reports."
```

Run all tests specified:
- Unit tests
- Integration tests
- E2E tests (if applicable)
- Linting/type checking

**Step 3: Create Test Report**

Create `.context-foundry/test-report.md`:

```markdown
# Test Report: {Project Name}

**Date**: {current_date}
**Iteration**: {test_iteration}

## Test Summary

**Status**: {PASSED or FAILED}
**Total Tests**: {count}
**Passed**: {count}
**Failed**: {count}

## Test Results

### Unit Tests
**Command**: {command}
**Status**: {PASSED/FAILED}
**Output**:
```
{test output}
```

### Integration Tests
**Command**: {command}
**Status**: {PASSED/FAILED}

## Failures (if any)

### Test: {test_name}
**File**: {file}:{line}
**Error**: {error message}
**Root Cause**: {analysis}
**Suggested Fix**: {what to do}

## Conclusion

{If FAILED: What needs to be fixed}
{If PASSED: All tests passed, ready for deployment}
```

**Step 4: Verify Output**

```bash
ls -la .context-foundry/test-report.md && echo "REPORT EXISTS" || echo "MISSING"
```

**DONE**: Your process exits. If tests failed, Architect fixes with fresh context.
