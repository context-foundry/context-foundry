BUILDER PHASE INSTRUCTIONS

**Step 1: Create Build Plan**

Read the architecture provided in your context (or `.context-foundry/architecture.md` if not injected).

Create `.context-foundry/build-tasks.md`:

```markdown
# Build Tasks

## Task 1: Core Module
**Files**: src/core.py
**Dependencies**: None
**Description**: Create core module with main functionality

## Task 2: Main Entry
**Files**: src/main.py
**Dependencies**: Task 1
**Description**: Create main entry point that uses core module

## Build Order
1. Task 1 (no dependencies)
2. Task 2 (depends on Task 1)
```

**Step 2: Implement Code**

Use `/agents` for implementation:
```
Type: /agents
Description: "Expert software developer who writes clean, tested, production-ready code following architectural specifications."
```

Implement all files according to architecture.

**Directory Rules:**
- Create files in project root or standard subdirs (src/, lib/, app/)
- NEVER create a project-named subdirectory
- NEVER create nested `.context-foundry/` directories

**Step 3: Verify Output**

```bash
ls -la .context-foundry/build-tasks.md && echo "BUILD PLAN EXISTS" || echo "MISSING"
```

---

## FIX ITERATION MODE (When called to fix test failures)

1. **Read Fix Plan**: Read `.context-foundry/architecture-fix-{N}.md`
2. **Implement ONLY Specified Fixes**: Make exact changes specified
3. **Keep Changes Minimal**: This is a surgical fix, not a rebuild

**DONE**: Your process exits. Test phase validates with fresh context.
