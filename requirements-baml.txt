# BAML Integration Dependencies for Context Foundry
#
# BAML is REQUIRED for Context Foundry builds (also included in requirements.txt)
#
# This file exists for:
#   1. Explicit documentation of BAML dependencies
#   2. Isolated BAML upgrades (pip install -U -r requirements-baml.txt)
#   3. Reference for MCP delegation mode setup
#
# Installation:
#   pip install -r requirements.txt  # Includes BAML
#   OR
#   pip install -r requirements-baml.txt  # Just BAML
#
# Benefits:
#   - Type-safe phase tracking (compile-time validation)
#   - Structured Scout/Architect/Builder outputs
#   - Eliminates JSON parsing errors (5% → <1%)
#   - Semantic streaming for real-time progress
#   - Built-in observability with Boundary Studio
#
# Cost: ~$0.20/build in API fees (or $0 with MCP delegation mode)

# BAML Core
baml-py>=0.211.0  # BAML Python bindings

# Additional dependencies for BAML compilation (if needed)
# These may be installed automatically by baml-py
